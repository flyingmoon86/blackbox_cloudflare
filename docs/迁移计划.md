# 黑匣子 Cloudflare 迁移计划

更新日期：2026-09-08

## 目标

将当前 Flask + MySQL + 本地文件存储的网站迁移为 Cloudflare 原生架构。

已确认的目标架构（2026-09-08）：

    用户浏览器
        |
    Cloudflare 单 Worker（TypeScript + Hono）
        |
        ├── 页面、路由、会话与业务逻辑
        |
        ├── D1
        |      SQLite 数据库
        |
        └── R2
               文件与视频存储

------------------------------------------------------------------------

# 当前系统状态

当前版本：

-   后端：Flask
-   模板：Jinja2
-   数据库：MySQL
-   ORM：Flask-SQLAlchemy
-   文件：本地目录存储
-   前端：Bootstrap + HTML + 少量 JavaScript

主要功能：

-   用户注册登录
-   邮箱验证与密码找回
-   名人堂
-   队员认证
-   作品管理
-   资源库
-   公告
-   献花
-   管理员后台

迁移目标：

-   保留现有业务逻辑
-   数据完整迁移
-   文件权限保持
-   去除服务器依赖
-   完整保留原 Python 网站，只作功能与数据迁移对照
-   所有页面和接口由同一个 TypeScript Worker 提供，减少部署单元和跨项目配置

------------------------------------------------------------------------

# Git Issue 总览

## Issue CF-001：建立 Cloudflare 项目基础环境

目标：

创建 Cloudflare 运行环境，为后续迁移准备。

内容：

-   创建 Cloudflare 项目
-   配置域名
-   创建 Workers 项目
-   创建 D1 数据库
-   创建 R2 Bucket
-   配置 Wrangler
-   建立开发环境变量

完成标准：

-   Cloudflare 环境可以正常部署
-   Worker、D1、R2 已关联

------------------------------------------------------------------------

## Issue CF-002：数据库 MySQL 迁移到 D1

目标：

将现有 MySQL 数据结构迁移到 Cloudflare D1。

涉及：

-   User
-   Member
-   JoinRequest
-   Announcement
-   Resource
-   Flower
-   EmailToken
-   Production
-   SiteProfile
-   PageText
-   关联表

任务：

-   分析现有数据库结构
-   创建 D1 schema
-   编写迁移 SQL
-   导出旧数据
-   转换数据格式
-   导入 D1
-   验证数据一致性

完成标准：

-   D1 可以替代原数据库
-   原用户、队员、作品、资源数据完整

------------------------------------------------------------------------

## Issue CF-003：Workers 后端框架建立

目标：

建立 Cloudflare Workers 后端基础结构。

内容：

-   创建 Worker 项目
-   建立 API 路由结构
-   配置 D1 连接
-   建立统一错误处理
-   建立权限验证机制
-   建立登录会话方案

确定结构（TypeScript）：

    src/
     routes/
       auth.ts
       member.ts
       production.ts
       resource.ts
       admin.ts
     services/
     database/
     middleware/

完成标准：

-   Worker 可以访问 D1
-   API 基础框架运行

------------------------------------------------------------------------

## Issue CF-004：账号系统迁移

目标：

迁移用户相关功能。

功能：

-   注册
-   登录
-   登出
-   密码修改
-   邮箱验证
-   密码找回
-   用户状态管理
-   角色权限

角色：

    user
    member
    admin

完成标准：

-   用户可以正常登录
-   权限逻辑与旧系统一致

------------------------------------------------------------------------

## Issue CF-005：名人堂系统迁移

目标：

迁移队员档案系统。

功能：

-   队员列表
-   搜索
-   年份筛选
-   分页
-   档案详情
-   照片展示
-   献花

完成标准：

-   名人堂页面功能完整
-   数据来自 D1

------------------------------------------------------------------------

## Issue CF-006：作品与资源系统迁移

目标：

迁移作品管理和资料库。

功能：

作品： - 剧目管理 - 演职员 - 后台成员 - 作品介绍

资源： - 文件记录 - 分类 - 审核状态 - 下载权限

完成标准：

-   作品页面正常
-   资源权限正确

------------------------------------------------------------------------

## Issue CF-007：R2 文件存储迁移

目标：

替代服务器本地上传目录。

迁移：

    旧：
    static/uploads
    static/resources

    新：
    R2 Bucket
    production/
    member/
    resource/

任务：

-   设计 R2 文件结构
-   数据库增加 R2 key
-   实现上传接口
-   实现下载接口
-   实现权限控制
-   支持大文件上传

重点：

视频文件： - 1GB+ - 5GB+

采用浏览器直传 R2。

完成标准：

-   文件不经过 Worker 中转
-   大文件上传可用

------------------------------------------------------------------------

## Issue CF-008：管理员后台迁移

目标：

迁移管理功能。

功能：

-   用户管理
-   队员审核
-   资源审核
-   公告管理
-   作品管理
-   网站设置

完成标准：

-   管理员可以完成日常维护

------------------------------------------------------------------------

## Issue CF-009：Worker 页面迁移

目标：

将 Flask Jinja 页面迁移为同一个 Worker 输出的 HTML 页面。

迁移：

    templates/
    ↓
    TypeScript Worker views

页面：

-   首页
-   登录注册
-   名人堂
-   作品
-   资源
-   个人中心
-   管理后台

技术：

第一阶段保持：

-   HTML
-   Bootstrap
-   JavaScript

不强制引入前端框架。

完成标准：

-   页面与业务路由由同一个 Worker 提供
-   静态资源通过 Worker 的 Assets 绑定提供

------------------------------------------------------------------------

## Issue CF-010：安全与上线检查

目标：

正式上线前检查。

检查：

-   未登录访问限制
-   管理员权限
-   文件下载权限
-   数据完整性
-   上传下载流程
-   域名 SSL
-   缓存策略
-   环境变量

完成标准：

-   Cloudflare 版本可以替代 Flask 版本运行

------------------------------------------------------------------------

## Issue CF-011：旧系统下线与清理

目标：

完成最终切换。

任务：

-   停止旧服务
-   保留数据库备份
-   保留上传文件备份
-   更新文档
-   更新部署说明

最终架构：

    Cloudflare TypeScript Worker
    +
    Cloudflare D1
    +
    Cloudflare R2

无需服务器。

------------------------------------------------------------------------

# 迁移原则

1.  不一次性重写全部代码。
2.  先迁移数据，再迁移业务。
3.  文件系统最后迁移。
4.  每完成一个 Issue 都保持可运行状态。
5.  保留旧 Flask 项目作为迁移期间参考。
6.  原 Python 网站不在迁移目录中改写；新实现只写入 blackbox_cloudflare。
7.  每个阶段先本地验证并记录结果，所有功能完成后再创建线上资源和切换流量。

------------------------------------------------------------------------

# 当前进度

-   阶段 0：Cloudflare 本地运行能力、D1、静态资源和旧密码兼容性验证完成。
-   阶段 1（账号基础）：正式 D1 表结构、TypeScript Worker、登录、退出、签名会话和 CSRF 已完成本地验证。
-   阶段 2A（账号操作）：注册、个人中心和修改密码已完成本地验证；用户选择暂缓邮箱发送与找回密码。
-   阶段 3A（队员功能）：队员名录、搜索筛选、档案详情、献花和认证申请已完成本地验证；管理员审核待后台阶段接入。
-   尚未上线，尚未连接真实 Cloudflare 账户、域名、邮件、旧数据库或真实文件。
