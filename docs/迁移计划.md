# 黑匣子 Cloudflare 迁移计划

更新日期：2026-09-10。

## 总目标

把 Flask + MySQL + 本地文件迁移为 TypeScript 单 Worker + D1 + R2。原 Python 网站保留在 `legacy-python/` 作功能对照，迁移完成并验证后再上线。

## 已完成

- [x] 建立独立 Cloudflare 迁移仓库和本地 Wrangler 环境。
- [x] 建立正式 D1 初始表结构。
- [x] 建立 Hono 单 Worker、静态资源、安全响应头和统一错误处理。
- [x] 登录、退出、签名 Cookie、CSRF、旧 Werkzeug scrypt 密码兼容。
- [x] 注册、个人中心、修改密码和 `auth_version` 会话失效。
- [x] 队员名录、搜索、年份筛选、档案详情和每日献花。
- [x] 绑定已有档案与申请新档案；新档案模式隐藏已有档案下拉。
- [x] 管理员审核申请、反馈结果、禁用与启用账号。
- [x] 作品列表、详情、创建编辑、演员角色和后台分工。
- [x] 公告发布删除、登录后首页入口、未登录访问限制。
- [x] 管理员指定精选大戏，管理首页、剧团和联系文案。
- [x] 资源列表与详情、作品归类、队员提交和管理员审核。
- [x] 作品详情集中展示该作品已审核入库的剧本、剧照、视频等关联资料。
- [x] 队员申请加入作品、管理员审核并写入演员或后台名单。
- [x] 管理员创建编辑作品时导入未归类的待审核或已入库资料。
- [x] 批量剧照提交、逐张审核，以及按作品分区的资料库和剧照画廊。
- [x] 可编辑测试须知、区分队员与访客欢迎语、网站意见箱和管理员收件箱。
- [x] 炭黑、暖白和舞台朱红视觉、登录注册页、权限说明页、完整登录导航和个人中心退出入口。
- [x] 队员修改本人简介与代表作，管理员维护完整队员档案。
- [x] 分级帮助页和两份可编辑指南，编辑入口直达相应字段。
- [x] 指南目录、任务跳转、队员操作步骤和队长审核快捷入口。
- [x] 队员可从作品档案页直接提交结构化建档申请。
- [x] 同角色多人饰演、AB 角提示、一人多角色或多分工及逐条审核。
- [x] 图片、视频、剧本、音频和其他文件的资料卡片与详情预览。
- [x] 资料库作品优先排序、组内提交时间排序，以及资料和作品联合搜索。
- [x] 登录首页快捷入口改为网站使用指南引导，队员档案入口统一为“修改我的信息”。
- [x] 首页紧凑首屏、统一公告与指南卡片，以及招新、介绍、联系信息三列整合。
- [x] 管理员直接新建队员档案、受确认保护的作品删除和资料保留规则。
- [x] 管理员解绑队员账号与档案，保留档案历史并使旧会话立即失效。
- [x] 管理员受保护地删除账号，保留队员档案和已审核资料。
- [x] 全站管理员待办通知、分管理员已读状态、四类展示上限和分组工作台。
- [x] R2 multipart、断点续传、取消、合并和过期清理；新后端版本逐片上传并增加恢复测试。
- [x] 审核后受控下载、Range 下载和管理员资源维护。
- [x] 队员头像上传、作品封面和首页背景剧照选择。
- [x] 首页小象留影、欢迎语换行、紧凑剧团页尾、全站模糊剧照背景和队员人物剧照卡。
- [x] 旧 MySQL 一致性导出、D1 转换、R2 文件迁移和数量外键校验工具。
- [x] 为任意已转换快照提供一次性内存 D1 导入验证命令。
- [x] 整理目录与当前文档；删除阶段零实验源文件。
- [x] 正式环境独立配置、必需 Secret 声明、防误部署检查、D1 备份和上线后冒烟检查。

## Cloudflare 正式上线状态

- [x] Cloudflare 账号登录，域名 `npublackbox.online` 已 Active。
- [x] 创建生产 D1 `blackbox-production`，初次执行 0001–0008 并导入确认保留的正式数据；2026-09-11 已继续执行 0009–0012。
- [x] 启用 R2，创建 `blackbox-files-production`，配置 CORS，上传 20 个正式对象（约 36MB）。
- [x] 仅配置 `SESSION_SECRET`，保持 Workers Free + Worker 绑定 R2 的免费模式。
- [x] 发布 Worker 自定义域名并完成冒烟检查：首页、健康检查、静态样式、作品权限和公告权限均通过。
- [ ] 内部队员验收真实登录、资料上传审核和手机端体验。
- [ ] 观察免费额度；新后端版本新增 9GB 上传预留限制（待上线），仍须观察 R2 操作数和 Workers 指标。
- [ ] 后续决定邮箱服务商并启用自助找回密码。

部署复现顺序：`npm run deploy:check` → `npm run deploy` → `npm run smoke:production`。生产数据库重大修改前先执行 `npm run db:production:backup`。

## 暂缓和取消

- 邮箱验证与密码找回：域名已经具备，但邮件服务商仍未确定；正式邀请队员前需要决定是先接入自助找回，还是由管理员临时处理遗忘密码。
- 奖项与演出季：用户决定不开发。

## 后端优化本轮状态

- [x] 第 0/1 包可靠性代码及 E 静态分流完成本地验收，30 项后端测试通过。
- [x] 优化前生产 SQL 快照在内存升级验证，保留 16 张原业务表全部原字段与行数。
- [x] 本地验收指南、架构与部署、验证记录及优化方案已同步。
- [ ] 发布窗口重新备份、预检重复、执行 0009–0011 并发布新代码。
- [ ] 上线后观察正常访问下的 CPU、数据库读写、R2 容量与操作数；完成大陆网络验收。

具体发布顺序以 [架构与部署](架构与部署.md) 为准，不能仅发布代码而漏掉新增迁移。


## 2026-09-11 前后端联合发布状态

0012（贡献者、匿名献花、新建议及审核触发器、资源重复指纹）与 0009–0011 已联合升级并发布。只追加结构，不覆盖生产业务数据或密码。认证及触发器解析问题均已解决。

发布顺序：最新生产 SQL 备份 → 内存兼容验证及重复数据预检 → 确认照片展示图 → 完成本地验收 → 应用增量迁移 → 发布 Worker → 生产冒烟与浏览器验证 → 更新真实上线状态。代码和文档分别中文提交。

## 联合发布结果（2026-09-11，最终状态）

- 已发布到 https://npublackbox.online/ 。Worker 版本：`5d630f67-a648-4e08-b1e4-cf519cb380c3`。
- 生产增量迁移 0009–0012 全部成功。触发器 CASE 使用括号兼容远程解析；最初失败已确认回滚，修复后重试成功。
- 部署前备份：`migration-work/backups/blackbox-production-2026-09-11T13-55-35Z.sql`。内存升级验证原字段/ID/密码哈希/引用/行数一致。发布后 14 个账号、7 份档案、2 部作品、10 份资料保持，外键检查为空。
- 38 项自动回归、格式、类型、转换和 dry-run 通过；上线后 9 项公开页面及后台保护冒烟全部通过。
- 本地截图与点击验收覆盖首页两幕、手机导航、队员名录和资料库。线上已核对浏览器首页内容树；截图通道超时，线上结果同时以 HTTP 冒烟和 D1 只读核验为证；不声称所有手机型号、缩放、慢网或大陆运营商均完成验收。
- 视频保持禁用；没有增加付费服务。重复指纹为辅助规则，旧资料未回算全量哈希。更多数据量的服务端分页、真实流量下费用/CPU 对比保留为后续工作。
