# 验证记录

## 2026-09-22 V3.5 正式发布

- 源码提交 `81608f1`，包版本 `3.5.0`；Worker `92ca8b60-7cce-437e-afa8-b3fc62c10fab` 已部署主站和独立后台。发布范围包括管理员分组、无编号模板、Excel 坐标误判修复和审核空间优化。
- `npm run deploy:check` 全部通过：格式、类型、100/100 后端测试及生产 dry-run；部署后 9/9 生产冒烟通过，额外确认后台工作台、导入选择和历史页未登录均跳转登录。
- 生产无待执行迁移，发布前 D1 私有备份 `migration-work/backups/blackbox-production-2026-09-22T14-30-11Z.sql` 已完成。此次仅部署代码和静态资源，不修改生产业务数据、密钥或 R2 文件。
- 浏览器证据复用下文已完成的双端/无 JS 合成流程；本次未使用生产账号提交或撤销真实导入。Git 提交与 `v3.5.0` 标签保存在本机，本轮未推送 GitHub。
- 完整归档见 [V3.5](releases/V3.5.md)。下文“仅本地”“尚未部署”为发布前的历史状态。

## 2026-09-22 管理员分组与无编号导入模板（仅本地）

- 工作台改为“待处理 / 填资料”。审核队列、账号和处理记录归入待处理；六个填写入口直接进入创建作品、新建队员档案、选择作品导入演职人员、上传资料、发布公告、网站页面编辑，不再绕到公开目录寻找表单。复用既有写接口与鉴权。
- 新增管理员 `GET /admin/credit-imports/new` 选择已有作品后进入原上传页；空列表提示创建作品，隐藏作品仅管理员可选，无效参数 400、不存在作品 404。
- v2 XLSX/CSV 仅保留姓名、类别、角色或分工三列；填写说明、示例、解析、预览、错误报告同步更新。唯一同名自动关联，缺失/多个同名须管理员确认；候选档案可打开核对。原表行号仅用于定位错误，不是档案编号。
- 保留四列 v1 文件、旧批次、已有编号与关系；按文件实际版本记录幂等键，旧文件重传仍返回原批次。新建档案的内部编号自动生成，无新增迁移，不修改业务数据。
- Automated：format、format:check、typecheck、全量 backend 100/100、git diff --check 通过；最后针对性导入 + Workers 12/12 复验通过。真实 Workers 用例改用 v2 XLSX，并验证并发确认只写一次及安全撤销；v1 XLSX/CSV 兼容继续由专项测试覆盖。
- Rendered：Chrome + 真实 Worker 路由 + 隔离合成数据库，1366×768 键盘 Enter、390×768 触屏、390×768 无 JS 完成六个填写入口访问及导入→预览→处理→确认→报告下载→撤销；减少动画开启。30 项记录，横向溢出 false，页面 JS 错误与失败 HTTP 请求均为 0。
- PC 首条待处理行 y=579–735px，完整进入 768px 首屏，已匹配行 122px。候选核对明细移至处理区之后，避免挤占首屏。手机自然滚动；测试点击前将目标滚动到固定底栏上方。已人工核对桌面/手机工作台与导入截图，模板两张工作表也已渲染核对。未进行真实 Microsoft Excel/WPS 编辑器交互验证。
- 前后证据：`evidence/2026-09-22-admin-import-before/` 与 `evidence/2026-09-22-admin-import-after/`；复验 `scripts/check-credit-import-browser.mjs`，可用 `EVIDENCE_DIR` 指定目录、`BROWSER_CASE=390-nojs` 单独复验无 JS。模板：`templates/production-credit-import-v2.xlsx`。
- 本地主目录开发服务已恢复到 `http://127.0.0.1:8787`，health 200。仅本地完成，未提交、推送或部署，未修改线上数据；工作区其他任务的改动保持不动。

## 2026-09-22 导入审核空间优化（本地）

- PC 预览分为左侧待处理、右侧已匹配/已处理；已匹配行默认显示关联信息，原生 details 展开后可调整。全部处理完成时清单使用全宽，结果页双列，手机单列。压缩标题与辅助链接，保留原有选择保存、确认和撤销流程。
- 1366×768 首条待处理行位于 y=579–743，选择框完整进入首屏；390px 与无 JS 流程均通过。24 项浏览器记录无横向溢出、脚本错误或失败请求；键盘展开调整、触屏处理、确认、下载报告及撤销通过。截图已核对，证据 `evidence/2026-09-22-import-review/`；旧截图保留在 09-21 目录。
- 格式、类型、全量后端 97/97 通过。使用合成数据验证，没有重新提交用户已完成的批次。改动仅在本地主目录，尚未提交或部署。


## 2026-09-22 演职人员批量导入（已发布）

- 新增 0019 增量迁移、XLSX/CSV 模板下载、管理员上传预览、匹配/新建/排除、原子入库、历史/错误报告、审计、受保护撤销和 180 天明细清理。只新增作品版本的演职关系；不处理照片，不创建账号，不覆盖原队员资料。
- Automated：全量后端 97/97 通过，包括 8 组导入专项和真实 Workers XLSX/并发确认/撤销；验证 50 行、未知/同名档案、冲突编号、AB角、重复文件/提交、失效会话/禁用/首次改密、CSRF/跨站、陈旧预览、事务失败整批回滚、压缩膨胀/坐标/公式防护、CSV 报告转义、过期清理与通知不计待办。
- Bugbot 复审发现并修补三处边界：工作表改名/重定位不能绕过稀疏坐标限制；无效超长字段先截断再生成去重键，避免把大字符串写入预览 JSON；队员搜索改用 `instr`，不再触发 D1 的 LIKE pattern 长度限制。针对性导入 + Workers 9/9 通过。
- `npm run deploy:check` 通过：格式、类型、全量后端 97/97 与生产 dry-run；Worker 695.19 KiB / gzip 171.12 KiB。
- Rendered：`scripts/check-credit-import-browser.mjs` 使用真实 Worker 路由 + 临时 HTTP 服务 + 合成内存库；1366×768 键盘 Enter、390×768 触屏及 390×768 无 JS 均完成上传→预览→新建/排除→重新检查→确认→下载报告→撤销。减少动画开启，21 组记录，无横向溢出、页面 JS 错误和失败 HTTP 请求。人工核对双端上传/预览/结果截图，结果提醒改为默认收起，避免误报待办和遮挡。桌面沿用现有后台内部滚动，移动端自然纵向页面；长页面截图中的固定底栏为正常视口定位。
- 证据：`evidence/2026-09-21-credit-import/`。模板两张表已渲染核对，移除不必要的条件格式公式；模板配置及 v1 表头受解析测试保护。
- 首轮完整回归发现测试端向 Miniflare 传递原生 FormData 的兼容问题；将测试请求显式序列化为标准 multipart 字节后，真实 Workers 解析通过。Windows 沙箱阻止 esbuild 读取目录，获准在本机运行隔离测试后完成复验。
- 发布前生产 D1 已备份到忽略目录 `migration-work/backups/blackbox-production-2026-09-22T01-52-51Z.sql`；随后仅增量应用 `0019_credit_imports.sql`，未重置或覆盖线上数据。迁移后无待执行项、外键检查为空，新表和字段为空，不会凭空产生导入记录。
- 代码提交 `5b0600d`，Worker 版本 `b5dac038-5a32-4ab7-bec7-6038e608dbe9`。9 项生产冒烟通过；主站公开页 200，公开域后台入口转独立后台，未登录后台再转登录页。未推送远程。依赖审计中已有开发链 sharp/miniflare/wrangler 提示保留，未以强制升级扩张本次范围。

## 2026-09-20 作品隐藏（已发布）

- 新增 0018：`production.is_hidden INTEGER NOT NULL DEFAULT 0 CHECK (is_hidden IN (0,1))`，复合索引 `(is_hidden, year DESC, id DESC)`。合成旧库升级后原作品默认公开；查询计划确认公开年份查询使用索引。未执行真实业务库迁移。
- 新增服务端可见性 SQL 辅助方法；作品列表/搜索/时间轴/详情/封面、首页精选/近期作品/展示图、队员作品与作品名搜索、资料搜索/个人提交/详情/文件、上传选项/初始化/续传/完成均检查父作品。普通用户及原上传者不例外。管理员更新接口使用现有会话角色、CSRF、同源校验；缺少新字段的旧编辑表单保留当前可见性。
- 管理员作品列表默认全部，提供可见性筛选、状态和显式切换按钮；编辑页使用原生单选开关，详情与时间轴标记隐藏，首页管理员预览也标记。公开用户不展示管理员控件。现有基础样式、URL、数据归属和审核状态不改变。
- 图片缓存仍只共享内部字节，权限先于缓存、HEAD、Range、304；响应使用 `private, no-cache` 避免用户间共享权限相关 URL。主题解析也过滤隐藏作品，隐藏详情带 noindex。没有新增 RSS、sitemap 或不存在的导出功能。
- Automated：`npm run format`、`npm run format:check`、`npm run typecheck`、`npm run test:backend` 通过，89/89 项（含真实 Workers 运行时）。新增覆盖旧库默认值/索引、三类非管理员身份所有现有入口、管理员筛选编辑切换/恢复、CSRF/非法值/越权、隐藏期间续传拒绝、预热缓存后的访问拒绝。初次运行因沙箱 esbuild 目录权限失败，获得本机测试权限后重跑；分页旧链接回归发现多余 visibility 参数，修正后全量通过。
- Rendered：本地临时服务 + 内存合成数据库，1366×768、390×768，管理员键盘 Enter / 手机触屏隐藏、筛选、详情标识、编辑页选择、游客列表与直接 404、无 JS 取消隐藏均通过；减少动画下功能完整，横向溢出 false，页面 JS 异常和 HTTP 失败均为 0（游客预期 404 单独断言）。已人工查看双端隐藏筛选截图。前后及详情/编辑/游客截图见 `evidence/2026-09-20-production-visibility/`。
- 已完成线上 D1 增量迁移、Worker 部署和 GitHub 推送：Worker `a1c2f6d7-b752-4869-97b8-26c1f2baa73e`，源码 `f922630`，备份 `migration-work/backups/blackbox-production-2026-09-20T08-55-08Z.sql`。没有修改现有作品可见性或其他业务数据；历史下载副本无法撤回，不能回滚到忽略隐藏字段的旧版本。

## 2026-09-20 幕布首帧与真实资源就绪

- 正式发布已完成：Worker `53ea575c-5d86-4700-bbeb-10a8c939a763`，源码 `d92cae3`，验收归档 `2cd7d52`。首次部署因网络中断失败，重新核对控制面仍为旧版后重试成功。发布前 D1 备份 `blackbox-production-2026-09-20T03-58-20Z.sql` 保存在忽略目录，未执行迁移或修改生产业务数据。
- 完整发布门禁通过（格式、类型、83/83 后端回归、生产构建），9/9 线上冒烟通过。正式 `/productions/991008` 在 1366×768、390×768 下人为延迟体验脚本，首帧均保持遮幕；放行后封面各请求一次、成功解码并正常开幕，无横向溢出、JS 异常或 HTTP 失败响应。线上截图为同目录 `*-live-before-script.png`、`*-live-open.png`。

- 原幕布由 defer 体验脚本创建，脚本到达前会先绘制页面背景。详情模板新增同源、带内容哈希的头部启动脚本，正文解析前显示不透明的闭幕底图；动态幕布插入后在同一任务交接。无 JS 保持原生封面；启动脚本失败时不补放迟到的幕布。
- 继续使用单次封面 XHR 的真实字节进度，等待封面解码、字体、页面加载及舞台背景图解码完成，再执行 4.2 秒开幕。慢网没有强制启幕计时器；启动阶段支持点击或 Esc 跳过，脚本错误退回原生页面。装饰背景或字体失败使用基础显示；封面失败保留错误反馈和手动跳过。
- 细化深红绒面与柔和褶皱高光，裁切噪声滤镜到布料本身；保留逐褶收拢。浏览器使用独立本地服务和合成 SSR：1366×768、390×768 延迟脚本/传输/解码/背景时不提前开幕，单次封面请求、无横向溢出、无页面 JS 异常；Esc、脚本失败、无 JS、减少动画通过。截图见 `evidence/2026-09-20-curtain-first-paint/`。

## 2026-09-20 公告木板正式发布

- 用户授权提交、部署和 GitHub 上传。代码 `c1412b1`、视觉记录 `03be332` 已合并主分支并推送；此前 V3 归档标签 `v3.0.0` 也已上传 GitHub，仍指向原 V3 归档提交。
- `npm run deploy:check` 通过：格式、类型、83 项后端回归与生产构建；9 项线上冒烟通过。生产无待执行迁移，发布前 D1 备份已保存到忽略目录 `migration-work/backups/blackbox-production-2026-09-20T03-23-44Z.sql`。首次查询遇到 7403，核对 OAuth 账号与权限后重试成功。
- Worker 版本 `a0e63d2e-7e33-49ba-b52e-e8c9dcaa3ed0`。真实线上 `/announcements` 和 `/announcements/9` 在 1366×768、390×768 均显示木板背景且无横向溢出。本轮只发布代码及静态资源，没有迁移或修改公告内容、账号、档案及媒体数据。

## 2026-09-20 公告页木板告示牌

- 公告列表改为木板底板与钉住的纸张；单条公告在板面居中，多条采用响应式双列/单列。详情沿用相同木板并把全文放在纸面上，保留管理员删除和返回入口；空状态也放在告示牌内。仅视觉与模板调整，无迁移、数据变更或线上部署。
- 本地合成公告浏览器渲染：1366×850、390×850 的列表和详情均 HTTP 200、无横向溢出；已人工核对截图。文件见 `evidence/2026-09-20-announcement-board/`。TypeScript 检查与 `git diff --check` 通过；未运行后端测试套件。

## 2026-09-19 V3 正式发布

- 用户明确授权归档 V3 并上线，发布源码 `54781c1`，包版本 `3.0.0`。Worker 版本 `6aa80fea-cd3d-4b64-bc12-a3b4d7f003d5`，主站与独立后台均已部署。
- 主项目运行 `npm run deploy:check` 全部通过：格式、类型、83/83 后端测试及生产 dry-run。先前 worktree 格式报告为换行差异；主项目 3 个文件重新格式化后无实际 Git 内容变化。
- 私有 D1 备份成功（665771 字节），仅应用 `0017_edition_backstage.sql` 的 4 个可空字段，无整库导入、旧记录回填、R2 文件操作或密钥变更。迁移前后账号40、档案72、作品23、版本26、资料204、演职员关系52、上传任务197，数量一致；迁移列表为空。
- 9 项线上 HTTP 冒烟通过。Chrome 游客 1366×768、390×768 的时间轴/详情无横向溢出、无页面 JS 异常，键盘 Enter 可打开作品，减少动画详情可用；无 JS 详情独立复验通过。截图见 `evidence/2026-09-19-v3/`，已人工核对桌面详情和手机时间轴。
- 浏览器点击导航捕获短暂 503 空响应，但同次导航最终200，直接访问也200；原因未确定，不声称网络零错误。首次无 JS 测试未等待导航即断言，修正等待，并独立复验实际详情可见。未在生产提交登录、上传、审核或删除测试，相关正常/失败路径以合成后端回归为证据。
- 完整发布和回退边界见 [V3 归档](releases/V3.md)。

## 2026-09-19 主导航一级入口可直达（本地验收完成）

- 三个分组标题已改为普通链接，目标分别是作品档案 `/productions`、队员名录 `/members`、鸣谢 `/thanks`；独立箭头按钮继续控制对应子菜单，并同步 `aria-expanded`、`aria-label` 和面板隐藏状态。链接沿用顶栏通用 hover 强调色。
- `node --check public/experience.js`、`git diff --check`、`npm run typecheck` 通过；改动的 JS/TS 文件单独运行 Prettier 检查通过。全仓 `npm run format:check` 仍报告 80 个文件不符合格式，未批量改写无关文件。
- 重试通过执行权限审核后，在当前工作树启动 `http://127.0.0.1:8791` 并初始化独立本地空数据库。游客在 1366×768 和 390×768 下逐一点击三个一级链接，均抵达目标路由；桌面悬停色为 `rgb(255,195,106)`，与该本地默认主题 `#ffc36a` 相同。手机触屏展开、桌面键盘 ArrowDown 进入子项与 Escape 关闭通过，无横向溢出或页面 JS 错误。截图已人工核对，见 `evidence/2026-09-19-navigation/`。补充修正展开箭头被通用按钮样式覆盖后颜色过暗的问题。未部署或推送。

## 2026-09-19 红绒参考落地（已按用户要求撤回）

用户要求恢复上一个幕布调整，已精确撤回本次渐变、滤镜、褶宽和下摆参数。真实加载、4.2 秒开幕与参考素材保留。以下为被撤回方案的历史验收记录。

- 按参考照片调整为正红亮面、酒红褶谷、低对比细绒纹；左右褶宽错开，增加下摆形变。保留 4.2 秒开幕与真实字节进度。滤镜使用原图透明度裁切，修复本轮截图发现的白色噪声边缘。
- 浏览器实测 1366×768、390×844，无横向溢出和 JS 错误；真实进度、慢网、超时、键盘/触屏跳过、减少动画、无 JS、未知总长度通过。截图已人工核对，位于 `evidence/2026-09-19-curtain-folds/`；`*-before-reference.png` 为此前存档。
- typecheck、format:check、backend tests（83/83）通过。本地完成，未部署或推送。

## 2026-09-19 幕布重量与逐褶收拢（替代刚性平移）

- 实际读取并逐帧核对 cottonbro studio 的 [Opening Curtain For The Start Of A Stage Play](https://www.pexels.com/video/opening-curtain-for-the-start-of-a-stage-play-6899898/)，视频长 28.72 秒，核对 0、1、2、4、6、10 秒帧：布面曲线与褶宽会变化，内侧边缘并非刚性直线。轨道侧边聚拢结构另参考 [ShowTex 轨道运动说明](https://www.showtex.com/en/knowledge-hub/types-track-movement)。参考视频仅用于观察，不作为站点素材。
- 原两张平移背景改为内联 SVG 独立褶片：桌面每侧 10 褶，手机每侧 6 褶；内侧牵引带动褶片逐步压密，下摆轻微滞后，侧边锚点保持遮挡。4.2 秒平顺启动/制动，无弹簧反弹；完成或跳过取消逐帧更新。保留真实下载、失败提示、音频选择、键盘、减少动画及无 JS 回退。
- 证据目录 `evidence/2026-09-19-curtain-folds/`：reference 为实拍参考帧，loading/opening/gathering/stacked/ready 为本地页面。测试额外断言开启中内侧褶宽小于外侧，验证确有折叠收拢。
- 最终桌面 1366×768、手机 390×844 截图已人工核对；褶宽压密、单次真实封面请求、限速/未知大小、慢加载/失败、键盘/触屏跳过、减少动画及无 JS 均通过，无横向溢出或脚本错误。格式、类型及后端 83/83 通过。本地原型未做真实手机 GPU 性能测量，未部署、未推送。
- 用户反馈幕布需要更红、更有毛绒感；本次仅调整绒面颜色、微纤维方向噪声和柔散高光，保留逐褶收拢轨迹与 4.2 秒节奏。

## 2026-09-19 真实下载进度与绒布启幕（替代下方初版）

- 取消固定三等份与 350ms 伪等待，封面单次 XHR 提供实际字节进度；同一响应 Blob 解码后展示，不额外发一次封面下载。百分比仅代表封面传输，HTML/字体/解码不混入估算；未知长度只显示已接收大小。260ms 插值始终追随已收到数据。
- 原创 SVG 表现不等宽褶皱、绒面细纹与顶部灯光，开幕 3.2 秒 linear。参考 [ShowTex DeLaMar 幕布](https://www.showtex.com/en/knowledge-hub/stage-curtains-theatres-thorough-guide) 与 [Gerriets 褶皱结构](https://www.gerriets.us/us/curtain-top-finish-webbing-us/)，未复制参考摄影。
- 浏览器证据目录 `evidence/2026-09-19-curtain-real/`；使用本地游客作品 991008，1366×768、390×844。脚本将网络限制为 128KB/s、延迟 200ms，核对中途百分比与原生 XHR progress 事件一致；额外本地 HTTP 代理分块响应去掉 Content-Length，验证只显示已接收字节且最终正常开幕。上线真实网络尚未验证。
- 最终复验：PC 捕获 2%、手机捕获 3% 的真实中间下载值，均与事件字节比一致；正常展示封面请求各 1 次。未知长度、慢响应、失败、键盘/触屏跳过、焦点恢复、减少动画、无 JS 封面通过；无横向溢出和页面脚本错误，桌面与手机截图已人工核对。format:check、typecheck、后端 83/83 通过。未部署、未推送。

## 2026-09-19 作品详情启幕（仅本地）

- 收尾修正关闭幕布时 Tab 可进入背后链接的问题：Tab/Shift+Tab 只在可见操作间切换，开幕隐藏按钮前将焦点移回正文；缺少音频 API 时禁用铃声按钮。同步修正交接文档“创新功能未开始”的过期状态。

- 普通作品详情进入/刷新时，用深色褶皱双幕呈现准备状态。底部居中铃铛及原生进度条按详情内容、封面解码、字体三个真实完成条件从 0/3 到 3/3；每项至少间隔 350ms 方便阅读，全部完成再停留 350ms，提示隐藏后 1600ms linear 匀速拉开。首字节等待无法由目标页脚本展示，不是下载百分比。
- 每步一次铃铛摆动和短双音提示；浏览器允许音频时播放，否则通过“开启铃声”授权，不补播旧步骤，可关闭。测试验证用户启用后后续步骤实际调度振荡器，未进行扬声器主观听感验收。8 秒慢加载提示和明确失败保留幕布，不伪造完成；可直接查看或 Escape 跳过。减少动画和无 JS 不生成幕布；深链接及返回不启幕。
- 仅移除作品详情顶部作品档案/资料库切换，保留返回作品档案和主导航，列表页入口不变。
- 1366×768、390×844 游客浏览器已验证加载等待、就绪移除、封面超时兜底、键盘/触屏跳过、减少动画，无横向溢出和脚本错误。测试人为阻塞/中止封面请求，不修改业务数据。截图 `evidence/2026-09-19-curtain/`，复验脚本 `scripts/check-work-curtain-browser.mjs`。
- 后端 83/83 通过；仅本地，未部署或推送。

## 2026-09-19 演出版本灯光转场（本地试点）

- 仅版本头部新增 400ms 渐亮及暖黄色灯光淡出；保留原生链接、URL、锚点和历史。sessionStorage 不可用则静态回退；刷新、返回和减少动画不触发。
- 游客实测本地 8788 端口 `/productions/991008`，1366×768 与 390×844：键盘 Enter / 触屏切换、返回、刷新、减少动画和无 JS 导航通过；无横向溢出、页面脚本错误和 HTTP 失败。
- 截图见 `evidence/2026-09-19-edition-light/` 的 before、after、light-frame 文件。light-frame 为测试暂停真实 CSS 动画在 100ms 的中间帧，已人工核对两尺寸；正常动画自动清理。手机无 JS 验收先将入口滚到视口中部，避免既有固定底栏遮挡。
- 后端 83/83 通过；复验脚本 `scripts/check-edition-light-browser.mjs`。本轮无迁移、未部署、未推送；局部效果等待用户确认后才推广。

## 2026-09-18 剧照返回路径与后台通知故障修复（未部署）

- 剧照详情增加同作品、同版本的上一张/下一张和位置总数；左右方向键可切换，带修饰键或位于输入/链接/媒体控件时不拦截。返回作品时保留服务端资料页和原剧照 ID，客户端恢复对应的六张一页视图。明显的随机文件名标题降级为“作品名 · 剧照 N”，原始资料标题仍在元数据中。
- 资料详情媒体保持文档流定位，桌面与手机均不再覆盖导航和正文；图片加载失败时隐藏不可用媒体并显示返回剧照集的恢复入口。新增服务端结构测试和客户端方向键/失败态测试。
- 真实本地管理员登录首次发现 `/admin/notifications` 返回 HTTP 500，页面按预期显示“待办同步失败（HTTP 500）”。根因是本地 D1 的复合查询上限为 5 项，而通知查询包含 6 项 `UNION ALL`；改为单行标量聚合后，服务日志连续返回 HTTP 200。
- 使用合成本地管理员验证正常通知请求；递增该账号 `auth_version` 后，轮询显示“登录状态已失效”和“重新登录”。临时账号测试后删除 1 条，本地数据未保留测试账号。未访问或修改正式数据。
- 浏览器实测《大国之蓝》：在剧照第 2/3 页打开第 7 张，右方向键切到第 8 张，返回后恢复第 2/3 页并显示原第 7 张。1366×768 与 390×844 均无横向溢出；图片为 `position: static`，媒体容器为 `position: relative`；手机上一张/下一张热区约 48px。
- `npm run typecheck`、`npm run format:check`、`npm run test:backend` 全部通过；完整后端测试 78/78。没有运行部署检查、没有部署或推送。

- 新增 `blackbox-fastpath`、`blackbox-evidence`、`blackbox-writeflow` 三个项目 Skill，分别压缩上下文接手、浏览器证据和权限写入验收。名称、描述和行数检查通过，均由 `opencode.json` 现有 `.agents/skills` 路径发现；Chrome DevTools MCP 配置未重复修改。

## 2026-09-18 后台待办同步诊断与故障恢复（本地）

- 已确认原故障链：管理员会话失效后，`/admin/notifications` 会 302 到登录页；浏览器跟随跳转后得到 HTTP 200 HTML，前端 JSON 解析失败又被空 `catch` 吞掉。已有待办数字会继续显示但不标记陈旧，可能掩盖真实提醒故障。
- 通知 GET/POST 在未登录与非管理员状态下统一返回 JSON 401/403，不再跳转或返回 HTML；正式后台域名和旧主站兼容入口都覆盖。普通后台页面原有登录跳转不变。
- 客户端显式验证 HTTP 状态、重定向、Content-Type 与 JSON 解析。会话失效显示“登录状态已失效 / 重新登录”；网络、超时、5xx 或解析失败保留最后一次有效待办数量，并附“待办同步失败（HTTP 状态码）/重试”。故障类型变化时记录一次不含响应正文、Cookie 或 URL 参数的控制台警告，恢复后清除故障状态。
- 自动化新增退出会话、普通账号、后台/主站双域、401/403 JSON、503 手动重试和登录页重定向识别。真实本地未登录请求返回 401、`application/json`、无 Location。
- 使用合成通知状态实际渲染并查看截图：1366×768 下待办数与故障提示并列，提示 237×45px、重试按钮 42×32px；390×844 下提示移到顶栏下方，页面 `scrollWidth=390`，无横向溢出。预览文件验收后已删除。
- `npm run format:check`、`npm run typecheck`、`npm run test:backend` 通过（75 项）。未使用真实管理员账号操作或写入业务数据，未部署、未推送。

## 2026-09-18 剧照详情返回与遮挡修复（本地）

- 根因是游客详情复用了资料卡片缩略图的绝对定位，图片脱离详情文档流并覆盖标题、导航和说明；详情页返回链接又只指向通用资料列表，丢失作品与版本上下文。
- 详情媒体已限定在预览容器内，桌面最高为 `min(72vh, 820px)`，手机最高 62dvh，保持原比例完整显示；不可断的长资料名允许换行，不再撑出横向滚动。
- 作品剧照的主返回入口改为“返回《作品名》剧照集”，深链到原作品、原版本和版本资料标题；其他资料仍保留原有资料列表/我的资料返回逻辑。
- 浏览器以游客身份实测资源 991076：桌面媒体从 `position:absolute` 修正为 `static`，媒体 720×449px、预览区 931×507px，文档宽度未超过视口；390px 浏览器覆盖（页面 `innerWidth` 325px）下媒体 238×149px，返回热区高 44px，无横向溢出。点击返回后到达 `/productions/991008?edition=6#edition-6-resources`，目标标题可见。桌面与手机截图均已人工查看，控制台无警告或错误。
- `npm run typecheck`、`npm run format:check`、`npm run test:backend` 通过（73 项）。登录后的原文件显示未使用账号实测，但同一媒体约束选择器覆盖登录态图片；未部署、未推送、未修改线上数据。

## 2026-09-18 作品详情首屏优化（本地）

- 桌面作品详情改为完整封面与节目单式信息双栏，作品名由首屏外 `y=915px` 提前到 `y=482px`；封面继续使用 `object-fit: contain`，不裁切原图。手机保持封面、标题和版本入口自然纵向排列。
- 新增“查看演出版本”页内入口，版本锚点为粘性顶栏预留 80px；390px 实测定位后版本导航顶部为 80px，版本按钮高度 45px。
- 版本内容增加年份、版本名称、演员/幕后人数和资料数量概览，名单按演员与幕后分组，资料入口改为 48px 高的明确操作区；桌面空幕后栏由与演员栏等高的 286px 缩为 69px。空版本显示零计数、“暂无记录”和“暂未收录资料”，仍保留补充资料出口。
- 剧照预览改用原生懒加载并保留完整 `src` 无脚本回退。13 张剧照的未访问作品首次打开只加载当前页 6 张，隐藏的 7 张保持未解码；切到第二页后加载第 7—12 张，第 13 张继续等待。剧照链接改为“查看《作品名》第 N 张剧照”，分页按钮具备明确名称和 44px 点击高度；横竖剧照按自然比例完整显示。
- 浏览器实测覆盖横版封面、竖版封面、无封面回退、1366×768、390×844 和 320×700。各状态无横向溢出；无封面作品显示主题色节目单背景，不保留空图片栏。控制台无警告或错误。
- `npm run typecheck`、`npm run format:check` 和生产 dry-run 构建通过。仅为本地源码与浏览器验收，未提交、推送或部署。

## 2026-09-17 边缘缓存优化正式发布

- 源码提交 39496d8、7fd0ba5；最终 Worker 1520bb7c-f13f-43e2-9094-290d7cf1c048。主站与管理员子域名均发布成功，Cron `17 * * * *` 保留。
- 发布前备份 `migration-work/backups/blackbox-production-2026-09-17T02-44-36Z.sql`（不进 Git）；`d1 migrations list` 返回无待执行迁移，未运行 migrate，未覆盖生产数据库或文件。
- `npm run deploy:check` 通过：格式、类型、73 项后端回归（含真实 Workers 运行时）与生产 dry-run；两次发布包分别 367.00 / 367.02 KiB，gzip 89.10 / 89.11 KiB。发布后 `npm run smoke:production` 9 项全部通过。
- 线上 HTTP 核对：`/site/theme.css` 返回 `text/css` + `public, no-cache, s-maxage=300`；`/site/hero`、`/site/poster` 返回 `image/jpeg` + `public, no-cache`；首页仍为 `private, no-store`。区域 Browser Cache TTL 会把 `max-age` 改写成 4 小时，故样式不使用浏览器 TTL。
- 新增 `src/services/edge-cache.ts`：封装 Cloudflare Cache API（`caches.default`）；不可用时自动退化为不缓存，Node 测试无需填充全局。
- 公共图片：`serveResourceFile` 对 `publicImage` 的 GET 且无 Range 请求，按 R2 对象键 + ETag 缓存字节 7 天。每次请求仍先做数据库权限与引用校验，只共享字节；已审核剧照预览标记为公共图片。撤回、待审不会因缓存放行（回归已覆盖）。
- `/site/theme.css`：按解析后的强调色缓存，边缘 300 秒；浏览器保持 `no-cache` 每次校验。改色生成新键，不命中旧样式。
- `site_profile` 只读配置：新增 `src/services/site-profile.ts` 的 `getSiteProfile`，Cache API 缓存 30 秒；管理员保存页面设置、删除资料清空首页图时主动失效。
- 未新增任何云资源绑定，仍为单 Worker / D1 / R2 / Static Assets；未引入 KV、Durable Objects 或付费服务。
- 尚未测量线上 TTFB、R2 Class B 用量与边缘/浏览器缓存命中率；跨数据中心改动最多等 TTL（图片/样式 300 秒、配置 30 秒）。

## 2026-09-15 前端二期与任务公告提醒正式发布

- 发布源码：72210e7；Worker：9523c8da-72bb-4f57-9f2c-80f85fac8b58。主站与管理员子域名均发布成功。
- 发布前格式、类型、数据转换及 69 项回归全部通过。线上无待执行迁移，未覆盖生产数据库或文件。
- 生产 D1 备份：migration-work/backups/blackbox-production-2026-09-15T13-33-46Z.sql（不进 Git）。发布前 Worker：2246766c-ea2e-4e7e-a633-28dffae38d1a，可作为代码回退参照。
- 线上首页、健康检查、样式、作品、公告、队员、资料、鸣谢与后台保护检查通过。另确认首页公告版本标记、作品搜索、作品原图返回及独立后台未登录跳转。
- 登录后管理员实际待办交互与手机视觉仍需人工验收；不能把未登录页面检查视为完整交互验收。下方“未部署”条目为当时的历史记录，本条更新当前发布状态。


## 2026-09-15 管理员持续待办与首页公告提示（本地）

通知接口新增实际待办总数与分组，已读通知不再等同于已完成任务。工作台服务端直接呈现任务横幅；前端保留页头醒目总数并在可见页面每分钟检查，审核后延迟合并刷新，请求超时保留旧提示。公告使用 ID、标题、正文与发布时间的 SHA-256 内容版本，按浏览器及账号记忆确认状态，并与测试须知顺序展示。无数据库迁移。

类型检查和 24 项公开/后台/通知客户端回归通过，包含通知已读仍待办、处理后清零、普通账号拒绝访问、公告正文更新、标题转义、账号隔离、确认后不再提示、等待测试须知、存储不可用。未完成浏览器视觉验收；未推送或部署。


## 2026-09-15 工作台入口精简（本地）

原三卡片改为待处理横栏、常用管理分组、折叠更多管理，保留原链接、计数、审核队列和账号操作。类型检查、16 项回归通过；渲染核对 14 个入口及账号管理均保留。仅改视图样式，视觉仍待本地验收，未部署。

## 2026-09-15 所有作品封面使用原图（本地）

作品列表与详情改用专用封面接口，游客可看已审核的选定封面原图，其他资料原图权限不变。类型检查通过，25 项作品与公开权限回归通过，包含撤回封面后拒绝访问。未部署。

## 2026-09-15 作品与队员背景明暗调整（本地）

移除两栏目的图片低透明度，改为顶部 28% 到底部 97% 的渐变暗色遮罩，保留原图细节。仅调整样式，无数据或权限变化；资源构建通过，视觉待本地确认，未部署。

## 2026-09-15 展示图片原图与背景清晰度（本地）

首页和栏目配置展示图改为原图，新增仅解析管理员已选招新海报的专用接口，手机图可回退通用图。去掉背景 blur，保留光效和渐变。类型检查及 16 项公开权限/首页回归通过；新增验证返回原图字节、待审核图拒绝、普通资料原图仍需登录。视觉效果待浏览器人工验收，未部署。

## 2026-09-15 作品档案搜索（本地）

新增与队员一致的搜索控件，支持名称、简介、版本名和年份；服务端先筛选再分页，保留排序与关键词，提供清除和空结果提示。类型检查与 9 项作品回归通过，包含搜索分页和通配符字面匹配。未部署。


## 2026-09-15 本周明星（本地）

首页小象展示改为本周明星，加入主题色相框、倾斜和阴影；管理选图同步更名，保留原字段和图片。类型检查与首页回归通过，视觉待验收，未部署。


## 2026-09-15 页面编辑分区（本地）

按指定顺序重排七区，以细线和留白分隔；海报并入招新页面，保留字段名称、单表单保存和原有锚点，补上介绍正文锚点。类型检查与 14 项相关回归通过，未部署。


## 2026-09-15 作品封面清晰度（本地）

作品详情登录后加载原图，游客仍为公开预览；完整比例显示、加高、取消图片暗色遮罩，标题放到图片下方。类型与 8 项作品版本回归通过，未部署。


## 2026-09-15 心意记录精简（本地）

缩小心意记录，底部与左侧送花操作对齐；实名送花按累计数量取前三名，更多或匿名心意以省略号提示，取消此区分页。总花数与历史记录不变。类型检查、14 项相关回归通过；尚未进行实际截图验收，未部署。


## 2026-09-15 前端 2.0 全站本地改造

- 真实布局接入公开深色/后台浅色、三幕首页、票根登录、节目单作品卡、五列队员卡和左右分栏档案；保留原生登录、审核、上传权限及管理员自定义强调色。新默认颜色为灯光橙，不批量修改旧设置。
- 最新调整：个人页 PC 固定外框与区内分页；作品详情单版本展示及按版本查询资料；独立栏目背景、页面编辑字段精简、移除指南入口并保留旧链接跳转。
- 字体完成 32 个按需 WOFF2 分包（总计约 1.6MB），附 OFL 和来源说明；浏览器只请求命中的字符包，尚未实际测量首屏字体流量。可选离线生成脚本需要 fonttools，正常 npm 构建和网站运行不需要 Python。
- 新增背景审核与权限、所选版本分页隔离测试，相关 22 项回归通过；最终 deploy:check 已通过：62 项回归、类型、格式、数据转换和生产配置 dry-run；没有发布网站。电脑截图此前受到窗口范围审批限制，随后用户 Escape 停止电脑控制；PC/手机实际视觉验收仍未完成，不将源代码完成写成视觉通过。
- 本地服务已在 8794 重启；首页、队员、作品、鸣谢、登录均返回 200，旧 /help 返回 302。恢复默认色按钮已同步为灯光橙。
- 无数据库迁移，无生产数据或配置变更。旧全站背景/指南字段保留数据库兼容，当前编辑页与公共页面不再使用。第一批独立样例已移除，改为本地跳转真实首页。


## 第一批历史记录（已被全站改造替代，非当前验收状态）

- 建立 codex/frontend-2-0 分支。修复 design-proposal/auth.html 的 GET 密码提交，改为禁用样例输入和正式登录链接。
- 新增隔离的 theme-v2.css 基础组件和仅 development 环境的 /_design/theme 样例；显式纳入静态指纹构建，现有正式布局与主题不变。使用系统宋体，字体授权与分包、管理员动态色映射仍未完成。
- 当前已有 58 项回归及 deploy:check 通过；新增预览环境隔离测试单独验证。电脑界面工具报告原生连接不可用，桌面/手机截图验收待完成，未假称已验收。
- 计划仍处于第一批组件样例阶段，三幕首页、内页换肤及完整动效尚未实施。所有业务数据与云端配置未改。

## 2026-09-14 性能优化正式发布及初期线上对比

- 发布前只读确认 SESSION_SECRET 与 TURNSTILE_SECRET 均存在，未读取值。备份保存于 Git 忽略目录 migration-work/backups/blackbox-production-2026-09-14T00-58-11Z.sql；本次无数据库迁移或业务数据覆盖。
- 先发布代码优化和 Turnstile：34ce6572-577d-403a-9e17-1ab53f2bab9f；再单独启用 Smart Placement：2246766c-ea2e-4e7e-a633-28dffae38d1a。最终两个域名均保留，9 项线上冒烟通过。生产配置声明两个必需密钥，未改登录会话密钥。
- 每阶段当前机器各 5 次 curl 样本，TTFB 中位数：旧版首页 1.169s、作品 0.717s、health 0.446s；代码优化后首页 0.840s、作品 0.723s、health 0.705s；Smart 刚启用首页 1.119s、作品 1.378s、health 0.654s。网络及时间不同，样本少、非随机对照，不能将变化全部归因于代码或 Smart Placement。
- Smart 初期 Cf-Placement 全部 local-LAX/local-SJC，尚未观察到转移；当前网络可能经过代理，不代表大陆直连用户。health 服务端处理 0ms，但 TTFB 约 0.43—0.75s；首页服务端约 337—367ms。可以确认链路等待明显，不能由此精确拆分 DNS、TLS、代理与数据库时间。
- Turnstile 注册组件、Site Key 和 CSP 来源已通过 HTTP 核对；无效 token 提交返回 403，没有创建账号。真实浏览器工具首次遇额度审批拒绝，恢复后正式 URL 导航仍超时，故成功挑战与重放实测仍待完成，已请用户确认注册页挑战显示。
- Smart 保持观察配置，不宣称有效提速。后续在它完成位置分析且有真实访问量后复测；若持续没有收益再关闭。代码优化后首页初期样本改善约 28%，仅为本次测量现象。

## 2026-09-14 延迟优化与 Turnstile 接入准备（未部署）

- 当前机器单次公开 HTTPS 采样：health TTFB 0.693s（TLS 0.444s）、首页 1.361s、作品页 0.694s；仅为当前网络样本，无管理员测量，不代表大陆队员总体延迟，也未据此认定数据库是瓶颈。
- 增加 Server-Timing 总处理耗时；首页独立查询并行。审核通过协商 JSON 响应省去成功重定向及整页重载，仍执行原认证、CSRF、审核事务；失败不移除条目。其余业务表单包括送花目前仍保持原流程，未宣称全站都改成无刷新。
- 本地合成浏览器实测资料审批成功后显示 2/2、焦点 review-resource-3、URL 保持审核页；查看桌面结果。前一轮已验收的筛选和快捷理由保留。
- Turnstile 新增注册 signup、匿名建议 feedback 的前端组件与服务端核验、限制 CSP 来源，检查 success/action/hostname、token 长度和超时；缺配置与上游失败不放行。Site Key 已记录，Secret 未读取、未写入；启用开关仍 false。
- 新增模拟 Siteverify 成功、重复失败、action/hostname 不符、上游失败及 JSON 审核权限/重复操作回归。真实 Turnstile 成功与重放验收、Smart Placement 前后线上对比仍待后续，不等同于本地测试通过。
- 最终 deploy:check 通过：58 项回归、格式、类型、旧数据转换和生产 dry-run。最终手机 390×844 截图确认审核成功提示、下一条和“查看最新待办”入口；无生产写入或数据库迁移。

## 2026-09-14 审核操作优化（未部署）

- 队员认证、作品加入、资料审核共用筛选记忆、处理后定位和常用驳回理由。存储按账号与页面隔离；理由仅填入，下一条仍需管理员确认。没有数据库迁移或生产数据修改。
- `npm run deploy:check` 通过：56 项回归、格式、类型、旧数据转换及生产 dry-run；上传估计 360.63 KiB / gzip 86.95 KiB，此命令未发布。
- 本地合成资料实测：筛选剧照并搜索后刷新仍显示 2/3；选择理由没有提交，明确驳回后焦点为下一条；空理由拦截后可以通过；类别处理完显示 0/1，清除筛选恢复其余资料。修复改变筛选后旧定位提示残留，并复验通过。
- 查看 1366×768、390×844 审核页截图，手机无横向溢出；作品加入页确认独立筛选和对应理由。队员认证沿用同一增强脚本，本轮未单独逐项操作其浏览器审批流程。
- 正式网站仍为前一条记录的独立后台版本。本轮代码、文档分别提交，等待明确部署指令。

## 2026-09-13 独立后台入口上线

- 用户同意以方便管理为主，独立后台地址、保留现有登录。本轮实现 admin.npublackbox.online 专用登录与管理导航、紧凑待办布局和折叠账号区；仍为单 Worker / D1 / R2，无数据库迁移、无账号或文件搬动，不引入 Cloudflare Access。
- 56 项回归与 deploy:check 通过，包含普通账号无法获取后台会话、未登录拦截、旧入口跳转、禁用/降权失效、跨源写入拒绝、Host-only Secure HttpOnly Cookie，原有业务和真实 Workers 运行时回归通过。增加生产域名配置校验。
- 合成浏览器验收 1366×768、390×844：实际截图查看后台导航、紧凑待办、手机管理底栏与后台登录页；从作品返回工作台、打开账号页及退出回登录流程通过。未提交生产测试数据。
- Cloudflare 初次备份返回 10000；正常刷新 Wrangler 会话后重试成功。备份为 migration-work/backups/blackbox-production-2026-09-13T15-44-01Z.sql（Git 忽略），无迁移。
- 发布版本 f6e45d3a-1bb2-4921-abd8-9b057576b3db；两个 Custom Domain 绑定成功，上传 358.94 KiB / gzip 86.44 KiB。主站 9 项线上冒烟通过。
- 正式 HTTPS 核对：后台根路径 302 到登录，/login 返回 200 且含“后台登录”，未登录 /admin/site 返回带目标路径的登录跳转；主站 /admin 返回新后台域名，后台响应含 noindex,nofollow。
- 正式浏览器导航通道仍超时，未声称完成线上登录后的视觉验收；线上验证通过 HTTP，带身份流程使用本地合成账号验证。
- 会话最长 14 天沿用旧设置，两域 Cookie 独立；旧管理写请求和通知接口保留既有认证，未做免认证或无期限登录。代码与文档分别中文提交。


## 2026-09-13 版本资料整理与实名送花上线

- 完成指南固定目录、实名花数与游客匿名累计、作品年份分组、全宽详情、资料库隐藏剧照、版本资料必选、管理员同作品移动和演职员搜索批量提交。管理员自写指南正文保留，系统操作步骤同步更新。
- `deploy:check` 通过：54 项后端回归、真实 Workers 运行时、类型、格式、转换及生产 dry-run。覆盖默认版本迁移保留文件、AB 角批量登记、重复整批回滚、跨作品移动拒绝、实名计数和资料库过滤剧照。
- 浏览器合成数据实测 1366×768、390×844，并查看截图：表单无横向溢出、年份标题清晰、指南跳转后目录固定。队员搜索筛选、移动列表排除目标版本、移动后显示于目标版本、实名花数均已实测。未向正式站写验收数据。
- 发布前备份 `migration-work/backups/blackbox-production-2026-09-13T10-14-06Z.sql`（283,135 字节，不进 Git）。内存副本试迁移保留 30 账号、70 档案、22 作品、71 资料、20 演职员，外键检查通过。
- 本地和生产成功应用 0016。只补齐未分版本的作品资料和未完成上传，不覆盖已有版本，不改文件。生产缺失版本资料 0、外键异常 0；71 资料、30 账号保留，146 个原 R2 对象键与 ETag 一致。
- 正式版本 `cc962b66-495d-400b-939c-db0630fe3ea1`，100% 流量；上传 355.05 KiB / gzip 85.43 KiB。9 项线上冒烟通过，正式作品页含版本与全宽布局，年份分组、指南正文区域、资料库剧照入口和引用脚本均通过内容检查。
- 此前公开作品页仍旧的现象本次核查未复现，不推断其根因。正式浏览器导航通道超时，线上通过 HTTP 内容验证；本地视觉已截图验收，不将超时写成线上截图通过。
- 更新公告仅提供草稿，不发布到站内或发送给他人。


## 2026-09-13 作品多版本与 README 精简

源码实现一个作品一张卡，详情按年份倒序展示版本及独立演职员。创建作品可批量添加版本，管理员可追加、修改；队员申请和管理员审核按版本处理，兼容 AB 角、一人多角。资料上传与编辑可选版本，旧资料默认作品通用。

- `deploy:check` 通过：50 项后端回归、类型、格式、旧数据转换和生产 dry-run；真实 workerd/D1 验证批量版本原子保存与按版本上传完成。迁移测试确认旧密码、记录 ID、文件键与待审核申请保留，删除作品不删除资料文件。
- 浏览器实际检查 1366×768、390×844：版本区、独立演员表、手机新增版本表单；角色提示在两个版本间分别显示 2 人与 1 人；版本入口预选上传参数，改换作品后清除旧版本。已查看桌面与手机截图。合成验收数据未写入正式库。
- 生产备份：`migration-work/backups/blackbox-production-2026-09-13T07-31-49Z.sql`，260,380 字节，不进 Git。该备份在隔离 SQLite 成功试迁移，19 部作品生成 19 个初始版本，演职员均有版本，外键无异常。
- 首次远程迁移遇到 `incomplete input`；只读确认新表未创建、迁移记录仍到 0014。将验证触发器改为 `SELECT RAISE(...) WHERE NOT EXISTS(...)` 后，正式 0015 迁移成功。
- Cloudflare 已接收发布：首次 `e138b0e3-c577-4062-a257-84b3041950d9`；为排查公开页面仍旧的问题，同代码再次发布为 `8054e2dc-6dbe-47af-a10a-1f0bf81afcbf`，控制面显示 100% 流量。官方内容 API 确认包含多版本代码，自定义域名仍绑定 blackbox-production，区域额外 Worker 路由为空。
- 数据核对：账号 26→27、档案 67→67、作品 19→22、资料 70→70、演职员 13→14、加入申请 8→9；期间正式站有新数据。版本共 22 个，无失配演职员/申请，无外键错误。142 个原 R2 文件的键与 ETag 一致。没有导入本地整库或覆盖文件。
- 9 项通用线上冒烟通过，但截至本次核查，公开作品详情仍返回旧“相关资料”布局，未返回版本区域。**暂不能将多版本标记为已生效或完整线上验收通过**；待确认公开访问与控制面差异后补充结果。

README 改为面向首次阅读者的项目介绍：用途、主要功能、简明架构、启动与文件结构；详细发布操作放本地验收指南，产品约束仍集中于网站优化 Skill。代码与文档分开中文提交。

## 2026-09-13 本地同步与历史清理

用户授权线上完整数据同步本地并删除旧本地运行数据。隔离导入后，27 张表逐行哈希核对一致；132 个 R2 文件共 48,168,202 字节，逐文件大小/可用 ETag/回读 SHA-256 校验通过，所有资料和头像引用存在。核验后替换 `.wrangler/state/v3`，本地 D1/R2 均为 local，无直连 R2 凭据。本地首页、列表、鸣谢及背景图片均返回 200。快照含 16 用户、64 队员、17 作品、66 资料，后续本地与线上独立。

先删除 13 条旧部署记录；用户随后明确要求删除代码版本，再通过官方版本删除 API 删除 13 个非活动版本。版本列表 total_count=1，只保留 `0df66ed0-2937-48c0-9579-434ab1e3baeb`，当前部署保持 100% 流量。未修改 D1/R2/域名，未重新发布。失败 Build 日志和用量统计不是代码版本，没有声称清除。

用户已反馈断开 Git 仓库构建连接，未来手动发布。截图日志确认旧 CI 在构建成功后执行 `npx wrangler deploy`，未选择 production，错误使用本地数据库全零 ID，返回 10181。无需改中文提交；正确发布入口为 `npm run deploy`。

## 2026-09-13 认证提醒、致谢与上线加固（已发布）

- 生产版本：`0df66ed0-2937-48c0-9579-434ab1e3baeb`；新增迁移 `0014_password_change.sql`，旧账号字段默认 0。
- 恢复书签（发布前）：`000001a3-00000000-000050e5-4ffa8df9a70bf3ad6db17bf5787ba5bb`，受 D1 保留期约束，不包含 R2 备份。
- 数据核对：用户 13→16（新增三个管理员），队员 64、作品 17、资料 66 不变，外键检查为空。三个新账号角色为 admin、active，强制改密标记均为 1；无密码写入 Git。
- `deploy:check` 通过：46 项后端回归、格式/类型、旧数据转换和生产构建。新增强制改密读写保护、旧会话失效、跨站提交提前拒绝、认证返回路径、精选优先、献花总数排序、致谢转义及主题 CSS 单查询测试。
- Edge 合成数据检查：1366×768/600 高度切幕、正文超过原 28vh 仍完整显示；超长正文才弹窗；390px 认证提示/图片选择/鸣谢，无脚本异常与横向溢出。已查看桌面和手机截图。
- 线上 9 项冒烟通过，Worker 启动测量 14ms，gzip 81.11KiB；这些是部署指标，不代表大陆用户端到端延迟。
- Cloudflare Git 自动构建此前的失败仍缺少错误日志，不能据此判断原因；此次使用 Wrangler 发布成功。特别致谢正文等待管理员自行填写，测试文案没有写入正式库。

## 正式站基线

2026-09-11 前后端优化联合发布，生产迁移 0001—0012。2026-09-12 选图预览版本为 `2d9a9bc1-3e56-474f-b6e1-fe17553a3e25`；当时 39 项后端回归和 9 项线上冒烟通过。此前所有历史过程保留在 Git，不再把已经失效的阶段说明作为现状。

## 2026-09-12 Version 1.0（已发布）

新增 0013：作品主题色、头像展示图字段和资料排序索引。旧表、旧字段及历史迁移保留。

本轮修改包括长页作品/资料浏览、服务端分页、作品独立颜色、首页去重复文字与放大海报、精简页面管理、认证先选旧档案、自动关联参与作品、导航图标、固定年份选项、备选图片入口、静态资源指纹及头像展示图。

已做检查：
- 42 项后端回归全部通过，包含新增分页、作品主题权限/历史年份与代表作保留、头像预览记账清理测试。
- 完整 deploy:check 通过：格式、TypeScript、旧数据转换/外键、真实 workerd/D1/R2、生产 dry-run。构建包 330.38KiB，gzip 79.61KiB。
- Edge 本地合成数据检查通过：1366×768 的作品长页及 12 部作品、资料长页、第二幕海报；390px 备选图片、取消和逐步认证流程，无页面脚本异常。测得剧照约 401×267px，海报约 408×598px；实际截图已查看。
- 默认小象 31,378 字节 WebP 替代 2,650,242 字节 JPG，移除无引用的旧发布图。没有拿这项字节差代替线上速度结论。
- 网站优化 Skill 通过官方 quick_validate 校验；原重复计划已合并删除，架构与部署集中到 README。

用户确认部署后，正式迁移 0013 成功，Worker 版本为 `58306cba-bb98-44eb-9770-444c719cb7db`。9 项线上冒烟全部通过：首页、健康检查、静态样式、作品、公告、队员、资料、鸣谢、后台保护。

部署前后均为 13 个账号、62 份队员档案、8 部作品、66 份资料，外键检查均为空。没有覆盖业务数据或修改 R2 原文件。本次记录 D1 Time Travel 恢复书签 `0000016b-00000000-000050e4-b2a4c707a820756d6cc5e875a83e723e`，未重新导出完整生产数据库；书签有保留期限，也不保护 R2 对象。

后续仍需真实大陆网络和长期费用观察；旧头像全量回填没有执行，读取保持兼容。验收步骤见 [本地验收指南](本地验收指南.md)，架构/部署见 [README](../README.md)。

## Version 1.0 后续修正

已发布首页第二幕禁止内部滚动、海报适配整屏高度、认证档案按年份倒序分组、横版作品加宽两列与按图片比例展示。长介绍可通过弹窗完整阅读，不影响滚轮切幕。

本地 Edge 检查了 1366×768 与 1366×600 双向滚轮切幕、第二幕 scrollTop 为 0、横版卡宽度超过 550px、390px 选图及认证年份 2026→2024→2020 顺序，并检查截图。42 项回归、类型、格式、数据转换和生产 dry-run 通过。本次没有数据库迁移。

正式 Worker 版本：`ad4e6489-4835-469d-a1ec-a7b073b9e0e2`，发布后公开页面与后台保护 9 项冒烟通过。Git 的 v1.0 标签保留首次正式版位置，后续修正记在 main 历史中。

## 2026-09-18 演出版本页签与渐进增强剧照灯箱（仅本地）

- 演出版本仍采用服务端 URL 切换，一次只渲染一个版本；当前版本改用 `aria-current="page"`，无 JavaScript 时功能不变。
- 作品剧照链接保留原详情 URL；支持 `<dialog>` 时增强为灯箱，可用按钮或左右方向键切换、Esc/关闭按钮退出，并在关闭后把焦点还给原缩略图。图片失败时显示可恢复说明，详情入口仍可用。
- 真实浏览器交互：16 张剧照由 1/16 切至 2/16，详情链接同步为下一张；关闭后焦点恢复；窄屏量算无横向溢出，关闭按钮右边界 510px，小于 535px 布局视口；控制台日志为空。
- 精确视口截图已视觉核对：`docs/evidence/2026-09-18-lightbox/production-1366x768.png` 与 `production-390x844.png`。1366×768 首屏构图完整；390×844 的封面、标题和底部导航无横向截断。
- `npm run typecheck`、`npm run format:check` 与 78 项 `npm run test:backend` 通过。未部署、未修改线上数据，也没有新增数据库迁移。
- 根据实机反馈，作品首屏恢复为“宽幅剧照在上、剧目信息在下”的海报式顺序，避免右栏挤压中文标题；版本详情去掉导航、版本标题、演职员标题和资料区的重复横线，改用留白与字号分层。补充核对截图：`hero-stacked-1366x768.png`、`hero-stacked-390x844.png`；窄屏边框量算均为 0px，页面无横向溢出。
- 后续按用户决定将顶部完全回退到已发布初版：图片 100% 单列、最大高度 85vh，标题字号恢复为 36–70px，去掉新增渐变海报块和封面黑色底；仅顶部回退。下方版本页签、剧照灯箱和去线规则继续保留，五处边框复测仍为 0px。当前证据为 `hero-deployed-1366x768.png` 与 `hero-deployed-390x844.png`。

## 2026-09-18 观众优先首页与 favicon（仅本地）

- 首页场景顺序调整为入场 → 近期作品 → 剧团介绍，保留精选作品、公告、剧照/资料等原有内容与路径。
- 根据用户反馈移除本轮新增的首页“浏览作品 / 找剧照与资料 / 认识剧团成员”按钮，不改变原有导航和作品入口。
- 页面 head 增加 favicon，`/favicon.ico` 增加兼容重定向，消除浏览器对默认图标的 404 请求。
- `npm run format:check`、`npm run typecheck`、78 项 `npm run test:backend` 与 `git diff --check` 通过；未部署、未修改线上数据、未新增数据库迁移。

## 2026-09-18 管理员队员档案删除与重名检查（仅本地）

- 新增 `POST /admin/members/:id/delete`，仅管理员可用；要求 CSRF 和输入完全一致的档案姓名，删除前将头像放入现有文件清理队列，再删除档案。
- 沿用现有外键规则：绑定账号解除档案关联，演职员、献花和队员资料关系按数据库级联清理；未引入迁移或直接删除 R2 对象。
- 新建和编辑档案均自动检查去首尾空格、不区分大小写的同名档案；重复时分别返回 409 并保留可读错误提示，编辑自身不会误判为重复。
- 覆盖管理员成功、普通账号拒绝、错误确认、不存在档案及新建/编辑重名路径。`npm run format:check`、`npm run typecheck`、79 项 `npm run test:backend` 与 `git diff --check` 通过；未部署、未修改线上数据。
## 2026-09-18 演出时间轴（仅本地）

- 新入口位于作品档案页，地址 `/productions?view=timeline`；沿用原路由、作品详情链接和权限，无迁移。按版本年份倒序，同年作品 ID 倒序，未知年份最后；跨年复排分别出现，同年多个版本合并。统计当年版本数、去重演职员数与已审核资料数。
- 封面仅使用本作品已审核剧照；缺封面时取当年首张有公开预览的已审核剧照。缺图显示紧凑提示，不生成空图片块。未添加转场动画。
- 格式、类型检查及完整后端 82/82 通过。修正旧剧照测试混用当前时间与固定时间导致的时序不稳定，只调整测试夹具。
- 本地游客真实浏览器：1366×768、390×844 均无横向溢出，17 个作品入口均可 Tab 到达，Enter 打开原详情。减少动画、手机触屏环境下检查通过；页面脚本错误和 HTTP 失败请求均为 0。
- 截图已视觉核对：`evidence/2026-09-18-timeline/1366-viewport.png`、`390-viewport.png`，同目录保留全页截图。复验脚本 `scripts/check-timeline-browser.mjs`（可用 PLAYWRIGHT_PATH 指定已安装的 Playwright）。未部署、未修改线上数据。
## 2026-09-18 第三步：剧照沉浸浏览（本地）

- 在现有原生 dialog 灯箱上补齐作品/版本/年份、背景滚动锁定、关闭时滚动及缩略图焦点恢复、浏览器返回关闭、手机水平滑动；继续使用公开预览和原详情链接，不改权限或数据库。
- 图片区域改为剩余空间布局；截图发现全站边距导致弹窗向右越界后，显式修正弹窗边距。1366×768 弹窗左右为 8/1358px，390×844 为 4/386px，均无内部横向溢出。未增加转场动画，本轮只做第三步。
- Playwright 本地游客实测 `/productions/991010`：方向键、Esc、焦点恢复、浏览器返回、关闭按钮、桌面遮罩关闭、手机 CDP 触摸滑动、减少动画、低高度 1366×500、模拟图片失败提示及无 JS 原详情回退通过。
- 截图已核对，见 `evidence/2026-09-18-immersive/before-1366.png`、`before-390.png`、`after-1366.png`、`after-390.png`、`after-low-height.png`。复验脚本：`scripts/check-immersive-browser.mjs`。
- 类型检查、格式检查、后端 82/82 通过。未部署；第四步已在后续记录中完成，等待第五步确认。
## 2026-09-18 第四步：版本补充信息（仅本地）

- 用户确认增量迁移后新增 0017：production_edition 的 creative_keywords、rehearsal_place、duration_minutes、backstage_story 均可空；已应用本地，未应用线上。旧记录不回填，旧表单缺字段保留已有值，明确清空则保存 NULL。
- 管理员在“编辑这个版本”填写排练地点（100 字）、正整数时长和补充说明（1000 字）。导演、编剧继续由演职员维护；创作关键词字段保留用于旧数据兼容，但不再出现在公开页面和新表单中。公开内容收束到版本头部的信息流中，作为弱层级的“补充信息”，仅展示非空项，全部为空时隐藏。
- 格式、类型及完整后端 83/83 通过，包含角色权限、CSRF、归属、非法输入、XSS 转义、版本隔离及旧表单兼容。
- 浏览器使用内存合成数据库生成真实 SSR 页面，在本地资源环境下检查 1366×768、390×844 的空、完整、部分长文本状态，无横向溢出；补充信息位于版本头部主信息下方，不再形成独立右侧大空栏。已核对截图 `evidence/2026-09-18-backstage/1366-full.png`、`390-full.png`。示例故事未写入本地业务档案；保存流程由请求级测试覆盖，未进行真实管理员浏览器登录提交。
- 本轮未部署。第五步等待用户确认。
