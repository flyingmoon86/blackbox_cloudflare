# 迁移验证记录

更新日期：2026-09-10。所有验证均使用本地模拟 D1 和虚拟数据；未连接正式账户、域名、邮件或真实文件。

## 基础与账号

- D1 初始迁移可重复执行，共包含业务表和大文件上传任务表。
- TypeScript 类型检查和 Wrangler 不发布构建通过。
- 旧 Werkzeug `scrypt:32768:8:1` 测试密码可验证。
- 匿名首页、静态 CSS、健康检查、登录、退出和签名会话通过。
- 修正根路由中间件误拦截静态资源的问题；CSS/JavaScript 返回正确类型，不存在的网址返回 404。
- 实际浏览器截图确认米白暖黄色主题和测试须知弹窗正常显示。
- 错误 CSRF 返回 400，错误密码返回 401 且不泄露账号状态。
- 注册会拦截弱密码、两次不一致和重复用户名。
- 修改密码后旧密码失效，`auth_version` 使旧会话立即失效。
- 格式错误的 Markdown 代理变量会由 `scripts/wrangler.mjs` 自动修正。

## 队员与管理员

- 未登录无法访问队员名录；搜索和年份组合筛选通过。
- 首次献花成功，当天再次献花被数据库唯一约束拦截。
- 绑定申请显示档案下拉；新档案申请不包含该下拉。
- 普通账号访问管理员页面返回 403。
- 绑定与新建申请均可通过；驳回理由会显示给申请人。
- 禁用账号后已有会话失效；管理员不能禁用自己。
- 个人中心退出入口可用；队员入口统一使用“修改我的信息”，管理员仍使用“编辑队员档案”。
- 队员名录改为整卡可点击的人物剧照卡；头像铺满卡片，鼠标悬停和键盘聚焦会快速虚化并上滑显示详情，无头像时使用姓名首字占位，手机端信息常显。

## 作品、公告与首页

- 管理员可创建编辑作品，添加或删除演员角色和后台分工。
- 普通用户可查看作品，但看不到管理操作。
- 作品详情会列出该作品已审核入库的剧本、剧照、视频等资料；待审核资料不会出现。
- 认证队员可提交演员或后台加入申请；管理员通过后角色进入作品名单，重复申请受到约束。
- 队员作品页“申请创建作品”入口、来源返回路径和管理员结构化收件箱通过。
- 隔离数据库验证同角色第二位演员、同时申请第二个角色、相同参与记录拦截和管理员同角色提示；批准后两名演员按角色合并并显示 AB 角标记。
- 创建作品可导入未归类的待审核和已入库资料，且关联不会绕过审核状态。
- 公告可发布删除，仅登录者可访问，入口位于首页而非顶部导航。
- 精选大戏由管理员从已有作品中选择；未登录点击先登录。
- 首页欢迎区和精选大戏使用紧凑双栏；公告与首次使用指南样式统一，网站建议入口归入首次使用卡片。
- 招新、关于我们和联系剧团改为紧凑的电脑端三列深色页尾；联系区保留邮箱文字链接和 QQ 群，不再显示微信、公众号、写邮件与复制邮箱按钮。
- 全站颜色更新为炭黑、暖白与舞台朱红，关键按钮增加悬停、聚焦和方向反馈。
- 首页欢迎语可在“黑匣子”后自动换行；实图验收确认戴眼镜的毛绒小象以“后台留影”方式融入首屏右上。
- 新增独立的全站模糊背景剧照设置和公开图片接口；未选择时接口稳定返回 404，页面回退到暖白底色。
- 访客与普通账号、认证队员与管理员使用不同欢迎语；登录首页保留全部原有内容和权限。
- 测试须知会在内容变化后重新显示；认证队员意见可进入管理员收件箱并标记处理。

## 资料记录与审核

- 认证队员可提交资料信息，并归入一部作品或“其他资料”。
- 待审核资料仅提交人和管理员可见，不会提前进入资料库。
- 普通未认证账号提交资料返回 403。
- 管理员可通过或驳回；空理由驳回返回 400，驳回理由会显示给提交人。
- 审核通过后资料进入资料库，列表可进入受权限保护的详情页。
- 资料库按作品和其他资料分区，同一作品的剧照集中为一块画廊。
- 隔离数据验证作品分组在前、“其他资料”最后，组内最新提交优先；资料名、说明、文件名和作品名搜索通过，`%`、`_` 会按普通字符处理。
- 两张剧照连续上传后生成两条独立待审核资料，管理员可分别处理。
- 修正管理员路由中间件作用域，回归确认队员可访问作品和公告，但不能进入管理员工作台。

## 队员档案与帮助页

- 已认证队员可修改本人简介和代表作，不能通过自助表单修改姓名、年份或届别。
- 管理员可从队员详情直达编辑页，维护队员身份信息、简介和代表作。
- 未登录访问帮助页会先登录；普通登录用户只看到队员指南。
- 管理员可看到两份指南，并从每份文案旁直达页面管理的对应字段。
- 管理员保存自定义指南后，管理员指南不会泄露给普通队员。
- 队员指南目录锚点、四类任务入口和固定步骤通过；管理员额外显示六个管理快捷入口，自定义正文仍可独立编辑。
- 管理员可从队员名录和工作台直接新建未绑定账号的队员档案。
- 管理员账号列表提供带明确确认的档案解绑；服务端限制只处理已绑定的普通队员账号，解绑会提升认证版本使旧会话立即失效，并清理该账号待审核的作品加入申请。

## 管理员待办与删除保护

- 新增按管理员记录已读的待办通知表；队员认证、作品加入、资料审核、作品建档和网站建议按类型聚合，顶部最多展开四类。
- 通知“处理”动作先保存已读状态再进入对应页面，新同类申请使用新的通知键重新提醒。
- 管理员工作台按待处理、内容管理、网站设置分组并显示待办数量。
- 作品删除要求输入完整名称；内存 D1 验证资料保留并取消作品归类，演职员、加入申请和首页精选引用正确清理。
- 账号删除要求输入完整用户名；本地端到端测试完成临时账号注册、管理员删除和再次查询消失，当前管理员自删返回 400。指定测试账号 `123`、`游客`、`visiter1` 已从本地 D1 删除。

## R2 文件与图片

- 本地 R2 完成 53MB、两个 50MB 级分片的并行上传与合并。
- 任务快照保留已完成分片；浏览器会复用同一文件的本地任务并从断点继续。
- 缺少分片时完成请求返回 409，上传任务可以取消。
- 队员上传完成后资源保持待审核，审核前下载返回 404。
- 审核通过后完整下载返回 200，Range 下载返回 206 且字节内容一致。
- 管理员可编辑资料元数据、删除 R2 对象；删除剧照同步清除作品封面、首页首屏和全站背景引用。
- 过期任务通过 Cron 测试触发后变为 `expired`，未完成 multipart 已取消。
- PNG 头像上传和读取通过；伪造 PNG 返回 400，管理员移除后返回 404。
- 已审核剧照可作为作品封面和公开首页背景，未登录首页背景读取通过。
- 正式环境的 S3 签名直传代码已通过类型和构建检查；尚需真实 Cloudflare R2 凭据、CORS 和 1–5GB 文件实测。
- 隔离 R2 验证上传 JPEG 预览图、资料创建、图片详情、视频播放器、剧本文档入口、音频播放器和 Range 206；匿名访问内部资料先被登录中间件拦截。
- 资料库和作品相关资料均生成视觉卡片；视频有缩略图时使用缩略图，旧视频无缩略图时尝试显示早期画面。

## 正式部署准备

- 本地构建和 TypeScript 类型检查通过，正式环境与本地环境已经分开。
- `npm run deploy:check` 在 D1 ID、R2 Bucket 和域名仍为占位值时按预期停止，不会误发布。
- 正式配置声明四个必需 Secret，并提供生产 D1 迁移、R2 CORS、D1 导出备份、正式部署和上线后冒烟检查命令。
- CSP 已允许浏览器向 R2 签名地址直传和本地视频 Blob 截帧；增加 HSTS 与浏览器权限限制响应头。
- 尚未连接 Cloudflare 账号，因此真实域名、远程 D1/R2、Secret、1–5GB 直传和大陆网络访问仍待验证。

## 旧站数据迁移工具

- MySQL 导出使用一致性快照和固定表白名单，只读生成 JSON 与 SHA-256 清单。
- 转换工具保留主键与关联，规范枚举、日期、头像和资源对象键。
- 旧首页背景会转换为一条已审核剧照；旧帮助条目会合并到新帮助指南。
- 虚拟旧站快照成功导入内存 D1，账号、队员、申请、作品、资料、关联、公告、献花和剧团设置数量一致。
- `PRAGMA foreign_key_check` 无错误，三个旧文件生成了正确 R2 清单。
- `data:verify` 已对现有真实导出转换结果完成一次性内存导入：十类表数量一致，外键无错误，迁移警告为空。
- 现有 R2 清单包含头像、DOCX 资源和首页背景三项，三项都在原网站目录找到至少一个有效源文件。
- R2 文件工具已把三个虚拟旧文件写入本地 Bucket，完成后测试对象已清理。
- 真实 MySQL、真实旧文件和远程 R2 尚未接入，因此真实数据数量与文件完整性仍需在测试环境核验。

## 清理

每轮端到端测试完成后，虚拟账号、队员、申请、献花、作品、演职员、公告和资源记录均已删除；剧团设置恢复默认值。
