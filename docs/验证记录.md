# 验证记录

## 2026-09-13 本地同步与历史清理

用户授权线上完整数据同步本地并删除旧本地运行数据。隔离导入后，27 张表逐行哈希核对一致；132 个 R2 文件共 48,168,202 字节，逐文件大小/可用 ETag/回读 SHA-256 校验通过，所有资料和头像引用存在。核验后替换 `.wrangler/state/v3`，本地 D1/R2 均为 local，无直连 R2 凭据。本地首页、列表、鸣谢及背景图片均返回 200。快照含 16 用户、64 队员、17 作品、66 资料，后续本地与线上独立。

先删除 13 条旧部署记录；用户随后明确要求删除代码版本，再通过官方版本删除 API 删除 13 个非活动版本。版本列表 total_count=1，只保留 `0df66ed0-2937-48c0-9579-434ab1e3baeb`，当前部署保持 100% 流量。未修改 D1/R2/域名，未重新发布。失败 Build 日志和用量统计不是代码版本，没有声称清除。

用户已反馈断开 Git 仓库构建连接，未来手动发布。截图日志确认旧 CI 在构建成功后执行 `npx wrangler deploy`，未选择 production，错误使用本地数据库全零 ID，返回 10181。无需改中文提交；正确发布入口为 `npm run deploy`。

## 2026-09-13 认证提醒、致谢与上线加固（已发布）

- 生产版本：`0df66ed0-2937-48c0-9579-434ab1e3baeb`；新增迁移 `0014_password_change.sql`，旧账号字段默认 0。
- 恢复书签（发布前）：`000001a3-00000000-000050e5-4ffa8df9a70bf3ad6db17bf5787ba5bb`，受 D1 保留期约束，不包含 R2 备份。
- 数据核对：用户 13→16（新增三个管理员），队员 64、作品 17、资料 66 不变，外键检查为空。三个新账号角色为 admin、active，强制改密标记均为 1；无密码写入 Git。
- `deploy:check` 通过：46 项后端回归、格式/类型、旧数据转换和生产构建。新增强制改密读写保护、旧会话失效、跨站提交提前拒绝、认证返回路径、精选优先、献花总数排序、致谢转义及主题 CSS 单查询测试。
- Edge 合成数据检查：1366×768/600 高度切幕、正文超过原 28vh 仍完整显示；超长正文才弹窗；390px 认证提示/图片选择/鸣谢，无脚本异常与横向溢出。已查看桌面和手机截图。
- 线上 9 项冒烟通过，Worker 启动测量 14ms，gzip 81.11KiB；这些是部署指标，不代表大陆用户端到端延迟。
- Cloudflare Git 自动构建此前的失败仍缺少错误日志，不能据此判断原因；此次使用 Wrangler 发布成功。特别致谢正文等待管理员自行填写，测试文案没有写入正式库。

## 正式站基线

2026-09-11 前后端优化联合发布，生产迁移 0001—0012。2026-09-12 选图预览版本为 `2d9a9bc1-3e56-474f-b6e1-fe17553a3e25`；当时 39 项后端回归和 9 项线上冒烟通过。此前所有历史过程保留在 Git，不再把已经失效的阶段说明作为现状。

## 2026-09-12 Version 1.0（已发布）

新增 0013：作品主题色、头像展示图字段和资料排序索引。旧表、旧字段及历史迁移保留。

本轮修改包括长页作品/资料浏览、服务端分页、作品独立颜色、首页去重复文字与放大海报、精简页面管理、认证先选旧档案、自动关联参与作品、导航图标、固定年份选项、备选图片入口、静态资源指纹及头像展示图。

已做检查：
- 42 项后端回归全部通过，包含新增分页、作品主题权限/历史年份与代表作保留、头像预览记账清理测试。
- 完整 deploy:check 通过：格式、TypeScript、旧数据转换/外键、真实 workerd/D1/R2、生产 dry-run。构建包 330.38KiB，gzip 79.61KiB。
- Edge 本地合成数据检查通过：1366×768 的作品长页及 12 部作品、资料长页、第二幕海报；390px 备选图片、取消和逐步认证流程，无页面脚本异常。测得剧照约 401×267px，海报约 408×598px；实际截图已查看。
- 默认小象 31,378 字节 WebP 替代 2,650,242 字节 JPG，移除无引用的旧发布图。没有拿这项字节差代替线上速度结论。
- 网站优化 Skill 通过官方 quick_validate 校验；原重复计划已合并删除，架构与部署集中到 README。

用户确认部署后，正式迁移 0013 成功，Worker 版本为 `58306cba-bb98-44eb-9770-444c719cb7db`。9 项线上冒烟全部通过：首页、健康检查、静态样式、作品、公告、队员、资料、鸣谢、后台保护。

部署前后均为 13 个账号、62 份队员档案、8 部作品、66 份资料，外键检查均为空。没有覆盖业务数据或修改 R2 原文件。本次记录 D1 Time Travel 恢复书签 `0000016b-00000000-000050e4-b2a4c707a820756d6cc5e875a83e723e`，未重新导出完整生产数据库；书签有保留期限，也不保护 R2 对象。

后续仍需真实大陆网络和长期费用观察；旧头像全量回填没有执行，读取保持兼容。验收步骤见 [本地验收指南](本地验收指南.md)，架构/部署见 [README](../README.md)。

## Version 1.0 后续修正

已发布首页第二幕禁止内部滚动、海报适配整屏高度、认证档案按年份倒序分组、横版作品加宽两列与按图片比例展示。长介绍可通过弹窗完整阅读，不影响滚轮切幕。

本地 Edge 检查了 1366×768 与 1366×600 双向滚轮切幕、第二幕 scrollTop 为 0、横版卡宽度超过 550px、390px 选图及认证年份 2026→2024→2020 顺序，并检查截图。42 项回归、类型、格式、数据转换和生产 dry-run 通过。本次没有数据库迁移。

正式 Worker 版本：`ad4e6489-4835-469d-a1ec-a7b073b9e0e2`，发布后公开页面与后台保护 9 项冒烟通过。Git 的 v1.0 标签保留首次正式版位置，后续修正记在 main 历史中。
