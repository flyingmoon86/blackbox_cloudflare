# V3（版本三）发布归档

发布日期：2026-09-19。线上地址：https://npublackbox.online/；独立后台：https://admin.npublackbox.online/。

## 发布标识

- 包版本：`3.0.0`。
- 部署源码：`54781c1`（包含此前所有 V3 优化提交）。
- 归档标签：`v3.0.0`，指向本次发布记录与证据归档提交。
- Worker：`blackbox-production`。
- Cloudflare 版本：`6aa80fea-cd3d-4b64-bc12-a3b4d7f003d5`。
- 发布前线上版本：`1520bb7c-f13f-43e2-9094-290d7cf1c048`。
- Git 归档保存在本机；本轮未推送 Git 远程。网站通过手动发布已上线。

## 冻结范围

保持深色、米白与站点强调色及原有作品顶部设计；归档演出时间轴、剧照沉浸浏览、版本补充信息、图片悬停聚焦、真实封面加载与红绒启幕、局部版本灯光效果，以及已完成的内容精简和管理员档案管理改进。主导航三个标题可直达对应第一页面，箭头保留展开子菜单。

不引入前端框架或 ORM，不增加未经确认的首页入口。本归档不意味着继续重做基础设计。

## V2 数据兼容与备份

发布前已通过现有发布脚本导出生产 D1，文件位于主项目被 Git 忽略的 `migration-work/backups/blackbox-production-2026-09-19T15-32-12Z.sql`，大小 665771 字节。文件含私有数据，不提交、不公开；D1 备份不包含 R2 文件字节。

生产仅缺 `0017_edition_backstage.sql`，已增量应用：给 `production_edition` 增加可空的 `creative_keywords`、`rehearsal_place`、`duration_minutes`、`backstage_story`。旧记录保持 NULL，不补写虚假故事。旧表单不提供字段时保留原值。没有改变旧 ID、密码哈希、对象键或权限规则，没有上传本地数据库，没有重置 D1、修改生产密钥或主动操作 R2 文件。

迁移前后聚合核对一致：账号40、队员档案72、作品23、版本26、资料204、演职员关系52、上传任务197。该核对证明数量一致，不宣称对全部生产行或 R2 字节做了逐项比对。执行后无待应用迁移。

## 验收

- 主项目 `npm run deploy:check` 通过：全量格式、TypeScript、83/83 后端测试、生产构建。回归包含历史密码/数据兼容、版本补充信息权限及非法输入、上传/审核正常失败路径。
- `npm run smoke:production`：9/9 通过，覆盖首页、健康、样式、作品、公告、队员、资料、鸣谢和后台保护。
- 线上 Chrome 游客：1366×768、390×768，时间轴和详情无横向溢出、无页面 JS 异常；键盘 Enter 进入既有作品 URL；减少动画可浏览详情；无 JS 详情独立验证可用。
- 截图在 [本次证据目录](../evidence/2026-09-19-v3/)，桌面详情与手机时间轴已人工查看。此前细分功能验收仍见验证记录与对应历史证据。

## 风险与恢复边界

- 点击导航时捕获短暂 Cloudflare 503 空响应，同次导航最终200，直接访问复验200；根因未确定，保留后续调查项，不把它隐藏成完全无错误。
- 不以生产用户账号做写入测试；真实生产登录、上传、审核未在本轮端到端重测，使用合成后端测试覆盖。没有验证所有用户历史记录及全部媒体。
- 如需回退，先核对当前部署与故障，不删除新列、不重置数据库。优先发布兼容性修复；禁止回退到不理解 `edition_id` 的旧代码。恢复 D1 备份可能覆盖备份后的新增数据，必须另行确认，不能作为自动回退步骤。
