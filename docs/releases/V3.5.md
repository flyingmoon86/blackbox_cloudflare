# V3.5 发布归档

发布日期：2026-09-22。主站：https://npublackbox.online/；后台：https://admin.npublackbox.online/。

## 发布标识

- 包版本 `3.5.0`，源码提交 `81608f1`，归档标签 `v3.5.0`。
- Worker `blackbox-production`，Cloudflare 版本 `92ca8b60-7cce-437e-afa8-b3fc62c10fab`。
- 代码与文档分开提交；标签指向归档提交。本轮已部署，未推送 GitHub。

## 本批更新

- 管理员工作台分为“待处理 / 填资料”，六个填写入口直达实际操作界面，移除绕到公开目录的入口。
- 表格导入新增作品选择入口；v2 XLSX/CSV 只填写姓名、类别、角色或分工，不要求系统编号。
- 唯一同名自动关联；未建档或多个同名须明确处理，候选档案可打开核对。保持 50 行、5MB 限制。
- 兼容旧 v1 文件和批次幂等规则，内部编号自动生成，原记录和关联不变。修复标准 Excel 工作表命名空间被误判为坐标的问题。
- PC 审核左侧集中待处理项，右侧压缩已匹配项；全部处理后全宽核对，移动端单列，无 JS 保留基础流程。
- 延续此前已发布的作品隐藏、首帧遮幕、公告木板及演职导入事务/审计/安全撤销能力，不重做全站基础设计。

## 数据兼容

生产迁移已到 0019，本次查询无待执行项，不执行新迁移，不上传本地数据库，不重置 D1、不改密钥、不主动操作 R2 文件。

发布前生产 D1 已备份到 Git 忽略目录 `migration-work/backups/blackbox-production-2026-09-22T14-30-11Z.sql`。备份包含私有数据，不提交；不含 R2 文件字节。

部署前后只读计数一致：账号 40、队员 71、作品 24、资料 204。此次核对不是所有行和媒体字节的逐项比对。

## 验证

- 发布门禁：format:check、typecheck、100/100 后端测试、生产 dry-run 通过。构建 700.60 KiB / gzip 171.92 KiB。
- 上线后 9/9 HTTP 冒烟通过；独立后台工作台、导入入口与历史页未登录均转登录，未公开管理内容。
- 本地真实 Worker + Chrome + 隔离合成库：1366×768、390×768、390px 无 JS，30 条记录覆盖入口与导入、处理、确认、报告、撤销；无横向溢出、脚本异常或失败请求。
- 双端截图与量算见 [优化后证据](../evidence/2026-09-22-admin-import-after/)，[优化前证据](../evidence/2026-09-22-admin-import-before/) 及 [审核布局证据](../evidence/2026-09-22-import-review/)。截图已在功能验收时人工核对。

## 限制与恢复

未在生产操作真实管理员导入、撤销或编辑；写流程通过合成测试验证。未做 Microsoft Excel/WPS 编辑器交互验收。

发生问题优先发布兼容修复。不能回退到忽略作品隐藏字段的版本，也不能用不支持 v2 批次的代码处理新批次。恢复备份可能覆盖后续数据，必须另行确认，不自动恢复。
