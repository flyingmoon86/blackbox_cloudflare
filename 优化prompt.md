1. 是否确认继续保持现有技术栈，不引入 ORM、前端框架或独立导入服务？
2. 本功能实际导入范围是哪一种？
   - A（推荐）：同时导入队员档案和作品演职分工
   - B：只导入作品演职分工，队员必须事先存在
   - C：建立包含手机号、证件号、紧急联系人的完整人事档案
3. 一份表格如何关联作品？
   - A（推荐）：上传前管理员先选择一个“作品 + 演出版本”，表内只填人员和分工
   - B：一份表格可以包含多个作品及版本，每行都填写作品和版本
   - C：支持两种模式
4. 人员唯一标识使用什么？
   - A（推荐）：新增可空且唯一的 external_id；旧数据首次匹配时由管理员确认姓名映射
   - B：姓名 + 入学年份
   - C：手机号或邮箱
   - D：其他标识
   不建议只按姓名自动合并，同名会误伤；也不建议用证件号。
5. 重复和更新采用什么规则？
   - A（推荐）：新增人员 + 更新基础档案 + 新增演职分工；完全相同的分工跳过
   - B：只新增，重复全部跳过
   - C：只更新已有人员
   - D：重复即整批报错
   同一人在同一版本拥有多个角色时，我会保留为多条合法分工，而不是互相覆盖。
6. 是否采用“档案最小化字段”？
   - A（推荐）：外部 ID、姓名、入学年份、入队年份、简介、演员/后台、角色或分工
   - B：增加邮箱、手机号
   - C：增加出生日期、证件号、紧急联系人等敏感字段
   当前数据库没有 B/C 的字段。若选择 B/C，需要额外确认加密、访问范围、保留期限及删除机制。
7. 模板和解析能力：
   - A（推荐）：同时支持 XLSX 和 CSV；XLSX 含“演职人员导入”“填写说明”两个 sheet，CSV 搭配网页说明
   - B：首版仅 CSV
   - C：首版仅 XLSX
   Cloudflare Worker 原生不能解析 XLSX。选择 A/C 是否允许加入一个经过审查、限制体积的开源 XLSX 解析依赖？
8. 角色和部门如何管理？
   - A（推荐）：类别固定为“演员 / 后台与创作”，具体角色或分工为自由文本
   - B：角色、职务、部门全部使用后台字典
   - C：角色自由文本，部门使用字典
9. 照片和附件如何处理？
   - A（推荐）：批量导入不处理照片、合同和附件，导入后走现有头像/资料上传流程
   - B：只接受现有站内文件地址
   - C：允许远程链接并下载进 R2
10. 权限及账号关系：
    - A（推荐）：只有状态正常的管理员可直接预览和确认，不创建登录账号、不自动绑定账号
    - B：导入需要第二名管理员审批
    - C：导入时同时创建或绑定账号
11. 写入原子性：
    - A（推荐）：全有全无；任何错误都不写入，修正后重新确认
    - B：正确行写入，错误行跳过并生成报告
12. 回滚强度：
    - A（推荐）：支持“安全回滚”；只撤销此批创建且之后未被修改的数据，冲突项要求人工处理
    - B：只保留审计记录，不提供自动回滚
    - C：强制恢复导入前快照，可能覆盖后续人工修改
13. 单次实际数据规模通常是多少？
    - A（推荐）：不超过 1000 行、5MB
    - B：不超过 5000 行、10MB
    - C：更大，请提供大致行数
14. 是否需要首版支持额外能力？
    - A（推荐）：固定的版本化标准模板，不做任意字段映射，也不开放外部 API
    - B：需要上传后手动映射列名
    - C：需要对外 API
    - D：字段映射和 API 都需要
15. 文件、日志和通知策略是否接受以下默认值？
    - 不永久保存原始表格，只保存 SHA-256 指纹、操作人、批次结果和必要的变更快照
    - 导入历史保留 180 天，审计日志长期保留
    - 错误报告可下载 CSV
    - 首版只使用站内结果页及现有管理员通知，不发邮件
   如不接受，请说明原文件是否需要存入 R2、保留多久，以及需要哪种通知。